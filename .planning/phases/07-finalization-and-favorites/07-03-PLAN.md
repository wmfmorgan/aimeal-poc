---
phase: 07-finalization-and-favorites
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/routes/plan-page.tsx
  - src/routes/plan-page.test.tsx
  - src/components/generation/PlanFinalizationCard.tsx
  - src/components/generation/FinalizePlanConfirmation.tsx
  - src/components/generation/ShoppingListPanel.tsx
  - src/components/generation/ShoppingListGroup.tsx
  - src/components/generation/FavoritesPanel.tsx
  - src/components/generation/MealCard.tsx
  - src/components/generation/MealPlanGrid.tsx
  - src/components/generation/MealDetailFlyout.tsx
  - src/components/generation/plan-finalization.test.tsx
  - src/components/generation/meal-detail-flyout.test.tsx
autonomous: true
requirements:
  - FINAL-01
  - FINAL-02
  - FINAL-03
  - FAV-01
  - FAV-02
must_haves:
  truths:
    - "The existing `/plan/:id` route stays the durable workspace for finalization, shopping list, and favorites."
    - "Shopping list and favorites open as right-side panels using the same focus-managed shell as the flyout."
    - "Draft meals never appear favorited or locally saved."
    - "Finalized-state messaging makes the draft-meal discard rule explicit."
  artifacts:
    - path: "src/components/generation/PlanFinalizationCard.tsx"
      provides: "Plan-level finalization summary and CTA surface"
      exports: ["PlanFinalizationCard"]
    - path: "src/components/generation/ShoppingListPanel.tsx"
      provides: "Right-side shopping-list panel with copy action"
      exports: ["ShoppingListPanel"]
    - path: "src/components/generation/FavoritesPanel.tsx"
      provides: "Right-side persistent favorites library panel"
      exports: ["FavoritesPanel"]
  key_links:
    - from: "src/routes/plan-page.tsx"
      to: "src/hooks/use-meal-plan.ts"
      via: "route-owned finalization and favorite orchestration"
      pattern: "finalizePlan|saveFavorite"
    - from: "src/components/generation/MealDetailFlyout.tsx"
      to: "src/components/generation/FavoritesPanel.tsx"
      via: "shared right-side panel and saved-state language"
      pattern: "Save to favorites|Saved|Open favorites"
---

<objective>
Apply the Phase 7 interaction contract on `/plan/:id`: add the plan-level finalization surface, shopping-list and favorites panels, and recipe-backed favorite affordances in the grid and flyout without breaking the established editorial route model.

Purpose: Phase 7 only succeeds if users can commit a plan, access the grocery artifact, and save repeatable recipes without leaving the existing plan workspace.
Output: new plan-level/panel components plus route, card, and flyout integration.
</objective>

<preflight>
Do not execute until Phase 6 live Spoonacular verification is approved. If that upstream gate is still open, Phase 7 stays in planning state only.
</preflight>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-finalization-and-favorites/07-CONTEXT.md
@.planning/phases/07-finalization-and-favorites/07-UI-SPEC.md
@.planning/phases/07-finalization-and-favorites/07-VALIDATION.md
@.planning/phases/07-finalization-and-favorites/07-PATTERNS.md
@src/routes/plan-page.tsx
@src/components/generation/MealCard.tsx
@src/components/generation/MealDetailFlyout.tsx
@src/hooks/use-meal-plan.ts

<interfaces>
From 07-UI-SPEC copy contract:
```text
Primary CTA: "Finalize plan"
Secondary finalized CTA: "View shopping list"
Favorites entry CTA: "Open favorites"
Favorite action: "Save to favorites"
Favorite disabled helper: "Enrich this meal to save it"
Shopping-list copy action success: "Shopping list copied"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the plan-level finalization card and shopping-list panel to `/plan/:id`</name>
  <files>
    src/routes/plan-page.tsx
    src/routes/plan-page.test.tsx
    src/components/generation/PlanFinalizationCard.tsx
    src/components/generation/FinalizePlanConfirmation.tsx
    src/components/generation/ShoppingListPanel.tsx
    src/components/generation/ShoppingListGroup.tsx
    src/components/generation/plan-finalization.test.tsx
  </files>
  <read_first>
    - src/routes/plan-page.tsx
    - src/components/generation/MealDetailFlyout.tsx
    - .planning/phases/07-finalization-and-favorites/07-UI-SPEC.md
    - .planning/phases/07-finalization-and-favorites/07-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - `/plan/:id` shows a plan-level finalization surface before the grid or alongside the existing header shell.
    - The finalize confirmation states that draft meals will be removed and only enriched meals are kept.
    - Once finalized, the primary finalize CTA is replaced by a finalized summary plus `View shopping list`.
    - Shopping list opens in a right-side panel with grouped items and a `Copy shopping list` action.
    - Copy feedback is exposed visually and to assistive tech.
    - Route-level enrichment controls (`Select meals`, selection mode, and batch enrichment orchestration) are hidden or disabled once the plan is finalized.
  </acceptance_criteria>
  <action>
Extend `src/routes/plan-page.tsx` so it owns Phase 7 plan-level orchestration next to the existing flyout state: open/close the finalize confirmation, open/close the shopping-list panel, and call the new finalize mutation from `useMealPlan`. Add a dedicated `PlanFinalizationCard` component for the explanatory copy, status summary, and CTA surface. When the plan is already finalized, swap the primary action into a calmer finalized-state summary with `View shopping list`.

Add `FinalizePlanConfirmation`, `ShoppingListPanel`, and `ShoppingListGroup` as right-side or editorial shell components that reuse the existing flyout language instead of inventing a second dialog system. The shopping-list panel should render grouped, de-duplicated items and expose `Copy shopping list` with lightweight success feedback. Keep the discard warning explicit and aligned with the locked context; do not soften or hide it.

Treat finalization as a route-level mode shift, not just a button swap. Once `generation_status === 'finalized'`, retire the stale enrichment entry points in `plan-page.tsx`: hide or disable `Select meals`, clear or suppress selection mode, and stop advertising batch enrichment orchestration that the server will now reject. The finalized route should still support detail viewing, shopping-list access, and favorites browsing.
  </action>
  <verify>
    <automated>npx vitest run src/components/generation/plan-finalization.test.tsx src/routes/plan-page.test.tsx</automated>
  </verify>
  <done>
Users can finalize a plan from the existing route, understand the discard tradeoff, and open/copy the resulting shopping list without leaving the plan workspace.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recipe-backed favorite affordances in cards, flyout, and a persistent favorites panel</name>
  <files>
    src/routes/plan-page.tsx
    src/routes/plan-page.test.tsx
    src/components/generation/FavoritesPanel.tsx
    src/components/generation/MealCard.tsx
    src/components/generation/MealPlanGrid.tsx
    src/components/generation/MealDetailFlyout.tsx
    src/components/generation/plan-finalization.test.tsx
    src/components/generation/meal-detail-flyout.test.tsx
  </files>
  <read_first>
    - src/components/generation/MealCard.tsx
    - src/components/generation/MealDetailFlyout.tsx
    - .planning/phases/07-finalization-and-favorites/07-CONTEXT.md
    - .planning/phases/07-finalization-and-favorites/07-UI-SPEC.md
  </read_first>
  <behavior>
    - Enriched meals show an active `Save to favorites` affordance from the card or flyout.
    - Already-saved meals show a stable `Saved` state.
    - Draft meals do not expose a fake active save state; they remain disabled or explanatory.
    - `Open favorites` launches a panel-based favorites library, not a top-level route change.
    - Finalized plans do not render stale mutate affordances that the server would now reject.
  </behavior>
  <action>
Add a `FavoritesPanel` component using the same right-side panel shell and route-owned open/close pattern as the shopping-list panel and flyout. The panel should render saved recipes with lightweight metadata, a calm empty state, and the Phase 7 helper copy. Wire `Open favorites` from the plan route and optionally from the flyout only if it does not compete with primary recipe content.

Extend `MealCard` and `MealDetailFlyout` with recipe-backed favorite affordances. Enriched meals can save and display `Saved`; draft meals must remain unavailable with the explicit helper language from the UI contract. Keep the source of truth persisted through the route hook. Do not add client-only toggles that drift from `is_favorite` or the favorites library query.

Because Wave 1 adds finalized-state guards on older mutations, the UI should stop advertising stale actions on finalized plans. Update `MealPlanGrid.tsx`, `MealCard.tsx`, and `MealDetailFlyout.tsx` together so finalized plans no longer surface stale delete, regenerate, select, or enrich affordances, while detail viewing, shopping-list access, and favorites browsing remain available. Keep `meal-detail-flyout.test.tsx` in scope because the saved-state and finalized affordance changes alter the flyout contract directly.
  </action>
  <verify>
    <automated>npx vitest run src/components/generation/plan-finalization.test.tsx src/routes/plan-page.test.tsx src/components/generation/meal-detail-flyout.test.tsx</automated>
  </verify>
  <done>
Users can save enriched recipes from the plan workspace and browse the persistent library in a panel without leaving `/plan/:id`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted finalization state -> route/panel UI | Destructive plan-state changes become user-visible control flow |
| favorite saved state -> card/flyout affordances | Cross-plan persisted state is surfaced back into local plan actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | D | finalize/shopping-list UI | mitigate | Keep finalization route-owned, require explicit confirmation, and use accessible copy feedback for clipboard actions |
| T-07-06 | T | favorite affordances | mitigate | Gate active saves on enriched state only and drive all saved-state rendering from persisted queries |
</threat_model>

<verification>
- `npx vitest run src/components/generation/plan-finalization.test.tsx src/routes/plan-page.test.tsx src/components/generation/meal-detail-flyout.test.tsx`
- `rg -n "Finalize plan|View shopping list|Copy shopping list|Save to favorites|Saved|Open favorites" src/routes/plan-page.tsx src/components/generation`
</verification>

<success_criteria>
- The plan route remains the primary workspace while gaining finalization, shopping list, and favorites capabilities.
- Finalization behavior and copy are explicit about draft-meal removal.
- Favorite controls are persistent, recipe-backed, and unavailable for draft meals.
</success_criteria>

<output>
After completion, create `.planning/phases/07-finalization-and-favorites/07-03-SUMMARY.md`
</output>
