---
phase: 07-finalization-and-favorites
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - supabase/migrations/20260422000004_finalize_plan_rpc.sql
  - supabase/functions/trpc/index.ts
  - src/hooks/use-meal-plan.ts
  - src/hooks/use-meal-plan.test.ts
  - src/lib/generation/shopping-list.ts
  - src/lib/generation/favorites.ts
autonomous: true
requirements:
  - FINAL-01
  - FINAL-02
  - FINAL-03
  - FAV-01
  - FAV-02
must_haves:
  truths:
    - "Finalization is a server-owned plan state transition that persists shopping-list data and removes draft meals."
    - "Favorite saves require enriched meals and persist both current-plan state and cross-plan library state."
    - "The persisted plan read path returns all Phase 7 state needed by the route without client-side guesswork."
  artifacts:
    - path: "supabase/migrations/20260422000004_finalize_plan_rpc.sql"
      provides: "Transactional finalize RPC/function for shopping-list persistence, draft removal, and finalized-state update"
      contains: "create function"
    - path: "supabase/functions/trpc/index.ts"
      provides: "Phase 7 read/write procedures for finalization and favorites"
      exports: ["AppRouter"]
    - path: "src/hooks/use-meal-plan.ts"
      provides: "React Query wrappers for finalize, favorite save, and favorites library data"
      exports: ["useMealPlan"]
  key_links:
    - from: "supabase/functions/trpc/index.ts"
      to: "src/lib/generation/shopping-list.ts"
      via: "finalization aggregation contract"
      pattern: "buildShoppingList"
    - from: "src/hooks/use-meal-plan.ts"
      to: "supabase/functions/trpc/index.ts"
      via: "finalize and favorite mutation invalidation"
      pattern: "mealPlan.finalize|meal.saveFavorite|favorites.list"
---

<objective>
Implement the Phase 7 server and hook layer: finalize owned plans, generate and persist shopping lists, save enriched meals to the favorites library, and hydrate the route with finalized/favorite state.

Purpose: Phase 7 must keep finalization and favorite persistence authoritative on the server. The route should only orchestrate state, not invent it.
Output: extended `mealPlan.get`, server mutations/queries for finalization and favorites, and hook wrappers with correct invalidation.
</objective>

<preflight>
Do not execute until Phase 6 live Spoonacular verification is approved. If that gate is still open, Phase 7 remains planned-only.
</preflight>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-finalization-and-favorites/07-CONTEXT.md
@.planning/phases/07-finalization-and-favorites/07-RESEARCH.md
@.planning/phases/07-finalization-and-favorites/07-VALIDATION.md
@.planning/phases/07-finalization-and-favorites/07-PATTERNS.md
@src/hooks/use-meal-plan.ts
@supabase/functions/trpc/index.ts
@src/lib/generation/shopping-list.ts
@src/lib/generation/favorites.ts

<interfaces>
From src/hooks/use-meal-plan.ts:
```ts
export function useMealPlan(planId: string | undefined) {
  return {
    plan: query.data ?? null,
    deleteMeal,
    regenerateMeal,
    slotMutationStateByKey,
  };
}
```

From supabase/functions/trpc/index.ts:
```ts
mealPlan.get
meal.delete
meal.regenerate
meal.enrich
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the tRPC router with finalization and favorites procedures</name>
  <files>
    supabase/migrations/20260422000004_finalize_plan_rpc.sql
    supabase/functions/trpc/index.ts
    src/lib/generation/shopping-list.ts
    src/lib/generation/favorites.ts
  </files>
  <read_first>
    - supabase/migrations/20260419000001_initial_schema.sql
    - supabase/functions/trpc/index.ts
    - src/lib/generation/shopping-list.ts
    - src/lib/generation/favorites.ts
    - .planning/phases/07-finalization-and-favorites/07-CONTEXT.md
    - .planning/phases/07-finalization-and-favorites/07-RESEARCH.md
  </read_first>
  <behavior>
    - `mealPlan.get` returns `generation_status`, `shopping_list`, and meal `is_favorite`.
    - `mealPlan.finalize` finalizes only an owned plan, aggregates a shopping list from enriched meals, deletes draft meals from that plan, and persists the final plan state through one transactional database RPC/function.
    - `mealPlan.finalize` rejects plans with zero enriched meals and returns a clear user-facing error.
    - `meal.saveFavorite` accepts `{ mealId: uuid }`, rejects draft meals, marks the meal row as favorite, and upserts the shared record in `favorite_meals`.
    - `favorites.list` returns the caller's saved recipes ordered by newest first.
    - Existing mutable plan procedures reject further draft-changing actions once the parent plan is finalized.
  </behavior>
  <action>
First create `supabase/migrations/20260422000004_finalize_plan_rpc.sql` with a database function or RPC that applies the destructive finalize transition transactionally: persist the computed shopping list, delete draft meals from the target plan, and set `generation_status = 'finalized'` in one database transaction. The function should reject plans with zero enriched meals and operate only on the target owned plan id supplied by the router.

Then extend `supabase/functions/trpc/index.ts` so Phase 7 state is fully owned by the router. Update `mealPlan.get` to return plan `generation_status`, plan `shopping_list`, and meal `is_favorite` along with the existing enriched fields. Add `mealPlan.finalize`, `meal.saveFavorite`, and `favorites.list`.

`mealPlan.finalize` must read only owned data through the caller JWT, build the shopping list through `buildShoppingList`, and call the finalize RPC/function so the destructive write path stays consistent if one sub-step fails. Keep the discard policy explicit in code comments and summary output. Do not implement finalization as a client-only mutation sequence or as three unrelated router writes.

`meal.saveFavorite` must allow enriched meals only. Use the helper layer to shape the favorite payload, persist `meals.is_favorite = true` on the current plan row, and upsert into `favorite_meals` keyed by `user_id + spoonacular_recipe_id`. Add a small `favorites.list` query so the route can open a persistent library panel without creating a detached page.

While touching the router, add a finalized-state guard to the older mutable procedures that would otherwise undermine finalization. `meal.enrich`, `meal.delete`, and `meal.regenerate` should reject attempts against plans whose `generation_status` is already `finalized`, returning a clear error the route can surface instead of silently mutating a committed plan.
  </action>
  <verify>
    <automated>rg -n "generation_status|shopping_list|is_favorite|mealPlan\\.finalize|meal\\.saveFavorite|favorites\\.list" supabase/functions/trpc/index.ts</automated>
  </verify>
  <done>
The server owns finalization and favorite persistence, and the persisted plan read path exposes the exact state the Phase 7 route needs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `useMealPlan` with finalize, favorite, and favorites-library invalidation</name>
  <files>
    src/hooks/use-meal-plan.ts
    src/hooks/use-meal-plan.test.ts
  </files>
  <read_first>
    - src/hooks/use-meal-plan.ts
    - src/hooks/use-meal-plan.test.ts
    - .planning/phases/07-finalization-and-favorites/07-PATTERNS.md
    - .planning/phases/07-finalization-and-favorites/07-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `useMealPlan` exposes `finalizePlan`, `saveFavorite`, and favorites-library query state in addition to the existing route actions.
    - Finalize and favorite mutations invalidate `mealPlanQueryKey(planId)` on success.
    - Favorite saves also invalidate the favorites-library query key so the panel stays current.
    - Hook tests cover finalize success/error, favorite-save eligibility handling, and favorites-library hydration.
  </acceptance_criteria>
  <action>
Extend `src/hooks/use-meal-plan.ts` instead of creating a parallel Phase 7 hook. Follow the existing stable query-key and invalidate-on-success pattern: add mutation wrappers for `mealPlan.finalize` and `meal.saveFavorite`, plus a separate query for the favorites library. Keep route-owned derived state minimal by returning the persisted plan fields directly and only layering on the mutation/query helpers needed for orchestration.

Update `src/hooks/use-meal-plan.test.ts` so the new wrappers are covered before the route UI is built. The tests should prove four things: finalize invalidates the active meal-plan query, favorite save invalidates both the plan and favorites-library queries, hook consumers receive clear errors when the server rejects finalize or save operations, and post-finalization mutate attempts surface the new finalized-state guard cleanly.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/use-meal-plan.test.ts</automated>
  </verify>
  <done>
The route can now orchestrate Phase 7 from a single hook using the same React Query patterns established in Phases 5 and 6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser finalize action -> tRPC router | Untrusted client intent triggers destructive plan-state changes |
| enriched meal row -> favorites library | Per-plan state crosses into a cross-plan persistent library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | E | `mealPlan.finalize` | mitigate | Scope reads and writes to owned plans through the caller JWT and reject finalize with zero enriched meals |
| T-07-04 | T | `meal.saveFavorite` | mitigate | Reject draft meals, keep recipe-backed saves server-side, and invalidate both plan and library reads after save |
</threat_model>

<verification>
- `npx vitest run src/hooks/use-meal-plan.test.ts`
- `rg -n "mealPlan\\.finalize|meal\\.saveFavorite|favorites\\.list|shopping_list|is_favorite|finalize_plan" supabase/functions/trpc/index.ts src/hooks/use-meal-plan.ts supabase/migrations/20260422000004_finalize_plan_rpc.sql`
</verification>

<success_criteria>
- Finalization is a server-owned, persisted state transition rather than a client illusion.
- The destructive finalize write is planned as one transactional database operation instead of loosely ordered router updates.
- Favorite saves persist both local meal state and cross-plan library state.
- Older draft-changing procedures no longer bypass finalization once a plan is committed.
- The route has one canonical hook surface for Phase 5-7 plan management behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-finalization-and-favorites/07-02-SUMMARY.md`
</output>
