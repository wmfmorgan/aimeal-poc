---
phase: 07-finalization-and-favorites
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260422000003_favorites_uniqueness.sql
  - src/lib/generation/types.ts
  - src/lib/generation/shopping-list.ts
  - src/lib/generation/shopping-list.test.ts
  - src/lib/generation/favorites.ts
  - src/lib/generation/favorites.test.ts
  - src/hooks/use-meal-plan.test.ts
  - src/components/generation/plan-finalization.test.tsx
  - tests/e2e/finalization-favorites.spec.ts
autonomous: true
requirements:
  - FINAL-02
  - FAV-01
  - FAV-02
must_haves:
  truths:
    - "Shopping-list aggregation is a pure, testable transform over stored enriched ingredient payloads."
    - "Favorite saves are recipe-backed only and idempotent by user + Spoonacular recipe id."
    - "Wave 1-3 work inherits stable shared types for finalized plan state, shopping-list groups, and favorite-library records."
  artifacts:
    - path: "supabase/migrations/20260422000003_favorites_uniqueness.sql"
      provides: "favorite_meals uniqueness guard for recipe-backed saves"
      contains: "create unique index"
    - path: "src/lib/generation/shopping-list.ts"
      provides: "Pure helpers for grouped shopping-list generation and copy text"
      exports: ["buildShoppingList", "buildShoppingListCopy"]
    - path: "src/lib/generation/favorites.ts"
      provides: "Pure helpers for favorite payload shaping and eligibility checks"
      exports: ["canFavoriteMeal", "buildFavoriteRecord"]
    - path: "src/lib/generation/types.ts"
      provides: "Shared finalized-plan, shopping-list, and favorites contracts"
  key_links:
    - from: "src/lib/generation/shopping-list.ts"
      to: "src/routes/plan-page.tsx"
      via: "server/client contract for grouped shopping-list display and copy text"
      pattern: "ShoppingList(Group|Item)|copy"
    - from: "src/lib/generation/favorites.ts"
      to: "supabase/functions/trpc/index.ts"
      via: "recipe-backed save contract"
      pattern: "spoonacular_recipe_id|is_favorite"
---

<objective>
Lay the Phase 7 foundation: add the minimum schema safeguard for favorite deduplication, define shared finalized/favorites contracts, and lock the shopping-list/favorite transforms behind tests before server or UI work depends on them.

Purpose: Phase 7 spans backend, route orchestration, and new panels. Wave 0 prevents ad hoc data shapes and duplicate-save behavior from leaking into later plans.
Output: uniqueness migration, shared helper modules, extended types, and executable test scaffolds for hooks/components/E2E.
</objective>

<preflight>
Do not execute this phase until Phase 6 live Spoonacular verification is approved. Phase 7 depends on Phase 6, and `STATE.md` still records that upstream gate as open.
</preflight>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-finalization-and-favorites/07-CONTEXT.md
@.planning/phases/07-finalization-and-favorites/07-RESEARCH.md
@.planning/phases/07-finalization-and-favorites/07-UI-SPEC.md
@.planning/phases/07-finalization-and-favorites/07-VALIDATION.md
@.planning/phases/07-finalization-and-favorites/07-PATTERNS.md
@supabase/migrations/20260419000001_initial_schema.sql
@src/lib/generation/types.ts

<interfaces>
From supabase/migrations/20260419000001_initial_schema.sql:
```sql
create table meal_plans (
  generation_status text default 'draft' check (generation_status in ('draft','enriching','finalized')),
  shopping_list jsonb
);

create table meals (
  status text default 'draft' check (status in ('draft','enriched')),
  is_favorite boolean default false
);

create table favorite_meals (
  user_id uuid references auth.users not null,
  spoonacular_recipe_id bigint
);
```

From src/lib/generation/types.ts:
```ts
export type PersistedMeal = {
  status: "draft" | "enriched";
  spoonacular_recipe_id?: number | null;
  ingredients?: unknown[] | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add favorite-library uniqueness guard and finalize shared Phase 7 types</name>
  <files>
    supabase/migrations/20260422000003_favorites_uniqueness.sql
    src/lib/generation/types.ts
  </files>
  <read_first>
    - supabase/migrations/20260419000001_initial_schema.sql
    - src/lib/generation/types.ts
    - .planning/phases/07-finalization-and-favorites/07-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - The migration adds a uniqueness guard for `favorite_meals(user_id, spoonacular_recipe_id)` that preserves idempotent saves for recipe-backed favorites.
    - `src/lib/generation/types.ts` gains plan-level finalized state and shopping-list contracts without breaking the existing persisted-plan route model.
    - Shared types cover plan `generation_status`, `shopping_list`, meal `is_favorite`, grouped shopping-list items, and favorites-library records.
  </acceptance_criteria>
  <action>
Create `supabase/migrations/20260422000003_favorites_uniqueness.sql` to enforce idempotent favorite saves at the database layer. Use the repo's existing migration style and keep the constraint scoped to recipe-backed favorites keyed by `spoonacular_recipe_id`; do not introduce a title-based uniqueness rule that would merge unrelated meals.

Extend `src/lib/generation/types.ts` with the Phase 7 read shapes the route and panels need: plan `generation_status`, optional `shopping_list`, meal `is_favorite`, grouped shopping-list item types, and favorite-library list entries. Preserve the existing Phase 5/6 persisted-plan shape and add fields in place rather than creating a parallel finalization-only plan type.
  </action>
  <verify>
    <automated>rg -n "favorite_meals|spoonacular_recipe_id|generation_status|shopping_list|is_favorite" supabase/migrations/20260422000003_favorites_uniqueness.sql src/lib/generation/types.ts</automated>
  </verify>
  <done>
The schema and shared contracts are ready for server and route work without duplicate-save ambiguity or ad hoc JSON parsing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build pure shopping-list and favorites helpers with Wave 0 test scaffolds</name>
  <files>
    src/lib/generation/shopping-list.ts
    src/lib/generation/shopping-list.test.ts
    src/lib/generation/favorites.ts
    src/lib/generation/favorites.test.ts
    src/hooks/use-meal-plan.test.ts
    src/components/generation/plan-finalization.test.tsx
    tests/e2e/finalization-favorites.spec.ts
  </files>
  <read_first>
    - .planning/spikes/005-spoonacular-recipe-shape/README.md
    - .planning/phases/07-finalization-and-favorites/07-CONTEXT.md
    - .planning/phases/07-finalization-and-favorites/07-VALIDATION.md
    - src/hooks/use-meal-plan.test.ts
  </read_first>
  <behavior>
    - Shopping-list helpers aggregate only enriched ingredient payloads.
    - Grouping prefers Spoonacular aisle/category data when present and falls back gracefully.
    - Quantity rollups combine only compatible units; incompatible units stay visible rather than being silently merged.
    - Favorite helpers reject draft meals and shape upsert-ready records from enriched payloads.
    - Wave 0 test files exist for hook, component, and E2E coverage named exactly as the validation contract expects.
  </behavior>
  <action>
Create `src/lib/generation/shopping-list.ts` as the pure transform layer that later plans will call from the server and panel UI. The helper should normalize ingredient identity, group items by category or a graceful fallback heading, roll up compatible quantities, and provide a copy-friendly plain-text representation suitable for clipboard use. Do not put database calls or UI-only formatting decisions in this module.

Create `src/lib/generation/favorites.ts` as the pure helper layer for favorite eligibility and favorite-record shaping. Restrict saves to enriched meals with real recipe ids and build the payload in the same snapshot style as the existing `favorite_meals` schema. Add real unit coverage in `shopping-list.test.ts` and `favorites.test.ts`. Also create or update the named Wave 0 test scaffolds in `src/hooks/use-meal-plan.test.ts`, `src/components/generation/plan-finalization.test.tsx`, and `tests/e2e/finalization-favorites.spec.ts` so later plans extend known files instead of inventing new test entry points.
  </action>
  <verify>
    <automated>npx vitest run src/lib/generation/shopping-list.test.ts src/lib/generation/favorites.test.ts</automated>
  </verify>
  <done>
Phase 7 has stable pure transforms, safe favorite eligibility rules, and predeclared validation entry points for the later execution waves.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored Spoonacular ingredient payload -> shopping-list transform | Third-party recipe fields become user-visible grocery data |
| enriched meal state -> favorite payload | Only recipe-backed meals should cross into the persistent favorites library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | shopping-list helper | mitigate | Normalize ingredient identity and only combine quantities when units are compatible |
| T-07-02 | T | favorite-library persistence | mitigate | Add a DB uniqueness guard and keep favorite payload shaping recipe-id based |
</threat_model>

<verification>
- `npx vitest run src/lib/generation/shopping-list.test.ts src/lib/generation/favorites.test.ts`
- `rg -n "shopping_list|is_favorite|favorite_meals|spoonacular_recipe_id" src/lib/generation/types.ts supabase/migrations/20260422000003_favorites_uniqueness.sql`
</verification>

<success_criteria>
- Phase 7 has a testable shopping-list transform before finalize mutations are written.
- Favorite saves are idempotent and recipe-backed at both the helper and schema level.
- Later plans can build on stable file names and shared contracts instead of inventing their own shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-finalization-and-favorites/07-01-SUMMARY.md`
</output>
