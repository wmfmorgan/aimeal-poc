---
phase: 07-finalization-and-favorites
plan: 04
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - src/hooks/use-meal-plan.test.ts
  - src/components/generation/plan-finalization.test.tsx
  - src/components/generation/meal-detail-flyout.test.tsx
  - src/routes/plan-page.test.tsx
  - tests/e2e/finalization-favorites.spec.ts
autonomous: false
requirements:
  - FINAL-01
  - FINAL-02
  - FINAL-03
  - FAV-01
  - FAV-02
must_haves:
  truths:
    - "Automated coverage proves mixed-state finalization, shopping-list rendering/copy, and favorites persistence across route revisit."
    - "Clipboard behavior, editorial panel quality, and disabled draft-favorite messaging are explicitly human-verified."
    - "Phase 7 closes with no detached route or client-only saved-state regressions."
  artifacts:
    - path: "tests/e2e/finalization-favorites.spec.ts"
      provides: "End-to-end regression suite for finalization and favorites"
    - path: "src/components/generation/plan-finalization.test.tsx"
      provides: "Component-level coverage for finalization card, panels, and copy feedback"
    - path: "src/hooks/use-meal-plan.test.ts"
      provides: "Hook-level coverage for finalize/favorite invalidation"
  key_links:
    - from: "tests/e2e/finalization-favorites.spec.ts"
      to: "src/routes/plan-page.tsx"
      via: "mixed-state finalize and persistent favorites journey"
      pattern: "Finalize plan|View shopping list|Save to favorites|Open favorites"
---

<objective>
Close Phase 7 with regression coverage and the required human verification pass for clipboard, panel quality, and cross-plan favorites behavior.

Purpose: Finalization and favorites both cross persistence and UI state. The phase is not done until the route, panels, and saved-state flows are verified together.
Output: green unit/E2E coverage plus one blocking human verification checkpoint.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-finalization-and-favorites/07-CONTEXT.md
@.planning/phases/07-finalization-and-favorites/07-VALIDATION.md
@.planning/phases/07-finalization-and-favorites/07-03-PLAN.md
@src/routes/plan-page.tsx
@tests/e2e
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish unit and end-to-end coverage for mixed-state finalization, copy flow, and favorites persistence</name>
  <files>
    src/hooks/use-meal-plan.test.ts
    src/components/generation/plan-finalization.test.tsx
    src/components/generation/meal-detail-flyout.test.tsx
    src/routes/plan-page.test.tsx
    tests/e2e/finalization-favorites.spec.ts
  </files>
  <read_first>
    - src/hooks/use-meal-plan.test.ts
    - src/components/generation/plan-finalization.test.tsx
    - src/routes/plan-page.test.tsx
    - tests/e2e/plan-management.spec.ts
    - .planning/phases/07-finalization-and-favorites/07-VALIDATION.md
  </read_first>
  <behavior>
    - Unit/component coverage proves finalize invalidation, favorite-save invalidation, panel rendering, and disabled draft-favorite messaging.
    - E2E covers finalize on a plan with mixed draft + enriched meals, verifies draft meals are removed after finalize, opens the shopping-list panel, and confirms copy feedback.
    - E2E covers saving an enriched meal to favorites, reopening the route or another plan, and confirming the favorites panel still shows the saved recipe.
  </behavior>
  <action>
Complete the validation contract already declared in `07-VALIDATION.md`. Extend the hook and component tests so they assert finalize, copy, and favorite flows in the same files declared during Wave 0. Then build `tests/e2e/finalization-favorites.spec.ts` to cover the end-to-end route path: start on a populated plan with mixed states, finalize it, confirm draft meals disappear while enriched meals remain, open the shopping-list panel, copy the list, save an enriched recipe to favorites, and reopen the favorites panel on a later route visit.

Encode failure coverage directly in tests. At minimum, cover finalize rejection when there are no enriched meals and confirm the UI surfaces the returned error without pretending the plan finalized. Also prove draft meals never expose a working save path even if the user opens the flyout.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/use-meal-plan.test.ts src/components/generation/plan-finalization.test.tsx src/components/generation/meal-detail-flyout.test.tsx src/routes/plan-page.test.tsx && npx playwright test tests/e2e/finalization-favorites.spec.ts</automated>
  </verify>
  <done>
Automated coverage proves the Phase 7 route works across finalize, shopping-list, and favorite-library behaviors without regressing the existing plan-management flow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification for clipboard flow and editorial panel quality</name>
  <what-built>
    `/plan/:id` now supports plan finalization, a grouped shopping-list panel with copy flow, recipe-backed favorite saves, and a persistent favorites library panel.
  </what-built>
  <how-to-verify>
    1. Start the local app with a user account that has at least one plan containing both draft and enriched meals.
    2. Open `/plan/:id` and confirm the new finalization surface reads as an extension of the existing route rather than a detached utility card.
    3. Click `Finalize plan`, read the confirmation copy, and confirm it plainly states that draft meals will be removed and only enriched meals will be kept.
    4. Finalize the plan and confirm the route now shows finalized-state messaging plus `View shopping list`.
    5. Open the shopping-list panel and confirm grouped grocery items feel readable on desktop and mobile widths.
    6. Click `Copy shopping list` and confirm both visible success feedback and real clipboard contents.
    7. Open an enriched meal and save it to favorites; confirm the saved state remains stable on the card and/or flyout.
    8. Open a draft meal and confirm the favorite affordance is unavailable with clear explanatory copy rather than looking broken.
    9. Open `Open favorites` and confirm the saved recipe appears in the panel without leaving `/plan/:id`.
  </how-to-verify>
  <resume-signal>Type "approved" if the live flow behaves as expected, or describe the failing step(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated route fixtures -> finalization/favorites flow | Tests must faithfully represent mixed-state plans and persisted favorite state |
| human verification -> clipboard and layout behavior | Real browser clipboard and editorial quality require direct observation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | R | regression coverage | mitigate | Encode finalize/favorites behaviors in executable unit and E2E tests, not prose only |
| T-07-08 | D | clipboard and panel usability | mitigate | Require one blocking human verification pass before the phase is treated as complete |
</threat_model>

<verification>
- `npx vitest run src/hooks/use-meal-plan.test.ts src/components/generation/plan-finalization.test.tsx src/components/generation/meal-detail-flyout.test.tsx src/routes/plan-page.test.tsx`
- `npx playwright test tests/e2e/finalization-favorites.spec.ts`
- Human verification checklist above
</verification>

<success_criteria>
- Automated coverage proves mixed-state finalization, shopping-list copy flow, and favorites persistence.
- Human verification confirms clipboard behavior and the editorial quality of the new panels.
- Phase 7 is ready for execution without unresolved route-model ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/07-finalization-and-favorites/07-04-SUMMARY.md`
</output>

<source_audit>
## Multi-Source Coverage Audit

### GOAL Coverage

| Source Item | Covered By |
|-------------|------------|
| Finalize a plan into a consolidated shopping list | `07-01` Task 2, `07-02` Task 1, `07-03` Task 1, `07-04` Task 1 |
| Persist favorite meals across plans | `07-01` Task 1, `07-02` Task 1, `07-03` Task 2, `07-04` Task 1 |

### REQ Coverage

| Requirement | Coverage |
|-------------|----------|
| FINAL-01 | `07-02` Task 1, `07-03` Task 1, `07-04` Task 1 |
| FINAL-02 | `07-01` Task 2, `07-02` Task 1, `07-03` Task 1, `07-04` Task 1 |
| FINAL-03 | `07-03` Task 1, `07-04` Task 1, `07-04` Task 2 |
| FAV-01 | `07-01` Task 2, `07-02` Task 1, `07-03` Task 2, `07-04` Task 1 |
| FAV-02 | `07-01` Task 1, `07-02` Task 1, `07-03` Task 2, `07-04` Task 1 |

### CONTEXT Coverage

| Decision | Coverage |
|----------|----------|
| D-01, D-02, D-03, D-04 | `07-02` Task 1, `07-03` Task 1, `07-04` Task 1 |
| D-05, D-06, D-07, D-08 | `07-01` Task 2, `07-02` Task 1, `07-03` Task 1 |
| D-09, D-10, D-11 | `07-01` Task 2, `07-02` Task 1, `07-03` Task 2 |
| D-12, D-13, D-14 | `07-02` Task 1, `07-03` Task 2 |
| D-15, D-16 | `07-03` Task 1, `07-03` Task 2, `07-04` Task 2 |
</source_audit>
