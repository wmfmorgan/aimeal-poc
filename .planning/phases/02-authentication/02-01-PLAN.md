---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env.example, package.json, package-lock.json, src/app/providers.tsx, src/app/router.tsx, src/lib/config/public-env.ts, src/lib/supabase/client.ts, src/lib/auth/auth-state.ts, src/components/auth/ProtectedRoute.tsx]
autonomous: true
requirements: [AUTH-02, AUTH-03]
user_setup: ["Run local Supabase services before browser-auth verification so the frontend can reach `/auth/v1/*` through Netlify"]
must_haves:
  truths:
    - "Signed-in state is available at the app root and survives browser restarts through Supabase's persisted session storage."
    - "Every route except `/auth` is blocked for signed-out users and redirects immediately to `/auth`."
    - "Signed-in users who visit `/auth` are redirected to `/household` instead of seeing the auth form."
  artifacts:
    - path: ".env.example"
      provides: "Repo-native documentation for frontend Supabase auth env vars used by Vite and Netlify dev"
      contains: "VITE_SUPABASE_URL"
    - path: "src/lib/config/public-env.ts"
      provides: "Validated access to browser-visible Supabase auth env vars"
      contains: "import.meta.env"
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client configured for persisted auth sessions"
      contains: "@supabase/supabase-js"
    - path: "src/app/providers.tsx"
      provides: "Shared app-level auth and query provider tree"
      contains: "AppProviders"
    - path: "src/components/auth/ProtectedRoute.tsx"
      provides: "Guard wrapper for protected routes"
      contains: "Navigate"
    - path: "src/app/router.tsx"
      provides: "Route table applying auth redirects to `/`, `/household`, `/plan/:id`, and `/auth`"
      contains: "/household"
  key_links:
    - from: ".env.example"
      to: "src/lib/config/public-env.ts"
      via: "documented Vite/Netlify dev env contract for browser auth"
      pattern: "VITE_SUPABASE_URL"
    - from: "src/lib/config/public-env.ts"
      to: "src/lib/supabase/client.ts"
      via: "validated Supabase URL and publishable key bootstrap"
      pattern: "VITE_SUPABASE"
    - from: "src/app/providers.tsx"
      to: "src/lib/supabase/client.ts"
      via: "shared browser auth client initialization"
      pattern: "createClient"
    - from: "src/app/router.tsx"
      to: "src/components/auth/ProtectedRoute.tsx"
      via: "route element wrapping for protected surfaces"
      pattern: "ProtectedRoute"
---

<objective>
Establish the browser-auth foundation and global route guards for Phase 2.

Purpose: Make Supabase Auth the app's source of truth before the `/auth` UI starts issuing sign-in, sign-up, recovery, and shell-level logout actions.
Output: A documented frontend env/bootstrap contract, a frontend Supabase auth client, shared session provider, protected-route wrapper, and router redirects.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-UI-SPEC.md
@.planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md
@architecture.md
@src/app/router.tsx
@src/app/layout/AppFrame.tsx
@src/app/providers.tsx
@supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 1 foundation — add the browser Supabase auth client and root auth-state provider</name>
  <files>.env.example, package.json, package-lock.json, src/lib/config/public-env.ts, src/lib/supabase/client.ts, src/lib/auth/auth-state.ts, src/app/providers.tsx</files>
  <read_first>.planning/phases/02-authentication/02-CONTEXT.md, architecture.md, src/app/providers.tsx</read_first>
  <action>Complete this stage before touching the router so later guard work depends on a single stable auth source of truth. Install `@supabase/supabase-js` and establish an explicit frontend env/bootstrap path for browser auth instead of relying on implicit shell state. Add `.env.example` entries for `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` (or `VITE_SUPABASE_ANON_KEY` if the repo keeps Supabase's legacy naming) so the repo records the required browser-visible values for Vite and `netlify dev`. Create `src/lib/config/public-env.ts` to read those `import.meta.env` values, fail fast with a clear developer error when missing, and export a normalized config object for the rest of the app. Then create the browser client module in `src/lib/supabase/client.ts` from that config and configure it for browser auth with `persistSession`, automatic token refresh, and URL-session detection so closed/reopened tabs restore the user's session per D-12. Create `src/lib/auth/auth-state.ts` to hold the app-facing auth context: `session`, `user`, `isLoading`, `isAuthenticated`, `signOut`, and any derived booleans the router and Plan 02 shell work need. Layer this auth provider into `src/app/providers.tsx` without replacing the existing React Query provider.</action>
  <verify>rg -n 'VITE_SUPABASE_URL|VITE_SUPABASE_(PUBLISHABLE_KEY|ANON_KEY)' .env.example src/lib/config/public-env.ts src/lib/supabase/client.ts && npm run build</verify>
  <acceptance_criteria>
    - `package.json` includes `@supabase/supabase-js`
    - `.env.example` documents the exact frontend Supabase auth vars expected by Vite and `netlify dev`
    - `src/lib/config/public-env.ts` is the single frontend bootstrap point for public Supabase auth config
    - `src/lib/supabase/client.ts` centralizes browser-client creation instead of scattering `createClient` calls
    - `src/lib/auth/auth-state.ts` exposes session/loading state and a `signOut` capability for the rest of the app
    - `src/app/providers.tsx` wraps children in both the query provider and the auth provider
  </acceptance_criteria>
  <done>The app has a repo-native browser auth env contract, a single frontend Supabase integration point, and root session state that can persist across browser restarts.</done>
</task>

<task type="auto">
  <name>Task 2: Stage 2 routing — apply the global protected-route and signed-in auth-route redirect rules</name>
  <files>src/app/router.tsx, src/components/auth/ProtectedRoute.tsx</files>
  <read_first>.planning/phases/02-authentication/02-CONTEXT.md, src/app/router.tsx</read_first>
  <action>Start this stage only after Task 1 is complete. Encode the locked rules from D-07 through D-10 directly inside `src/components/auth/ProtectedRoute.tsx` and the route table instead of introducing an extra helper module in this foundation plan. `ProtectedRoute` should consume the auth-state created in Task 1 so the redirect policy is centralized around one context, not split across ad hoc client calls. Update `src/app/router.tsx` so `/`, `/household`, and `/plan/:id` all pass through the guard while `/auth` performs the inverse redirect for authenticated sessions. Preserve intended destination in router state for future reuse, but still route successful auth to `/household` per D-04 and D-06.</action>
  <verify>rg -n 'ProtectedRoute|/auth|/household|Navigate' src/app/router.tsx src/components/auth/ProtectedRoute.tsx && npm run build</verify>
  <acceptance_criteria>
    - `src/app/router.tsx` no longer leaves `/`, `/household`, or `/plan/:id` publicly accessible
    - `src/components/auth/ProtectedRoute.tsx` uses auth-state loading/session information instead of duplicating client creation
    - the redirect policy lives in one guard path tied to the shared auth provider instead of being scattered across route elements
  </acceptance_criteria>
  <done>Route access is globally enforced and `/auth` behaves as a signed-out-only entry surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'VITE_SUPABASE_URL|VITE_SUPABASE_(PUBLISHABLE_KEY|ANON_KEY)' .env.example src/lib/config/public-env.ts src/lib/supabase/client.ts` confirms the frontend env/bootstrap path
- [ ] `npm run build` passes
- [ ] `rg -n 'ProtectedRoute|/auth|/household|Navigate' src/app/router.tsx src/components/auth/ProtectedRoute.tsx` confirms the redirect wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app has a documented frontend Supabase env/bootstrap path, a persistent browser session source of truth, and global protected-route enforcement aligned to the locked Phase 2 rules so Plan 02 can stay focused on the single `/auth` surface and shell logout
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-01-SUMMARY.md`
</output>
