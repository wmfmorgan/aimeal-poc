---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [supabase/config.toml, src/routes/auth-page.tsx, src/app/layout/AppFrame.tsx, src/lib/auth/auth-copy.ts, src/lib/auth/auth-state.ts, src/lib/auth/validation.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04]
user_setup: ["Keep local Supabase Inbucket available during verification so reset emails can be inspected at `http://127.0.0.1:54334`"]
must_haves:
  truths:
    - "The user can create an account and sign in from a single `/auth` surface with in-place mode switching."
    - "Successful sign-up and sign-in both send the user to `/household`."
    - "The user can request a password reset email and complete the new-password step inside the app rather than on a hosted Supabase page."
    - "The `/auth` page matches the approved editorial UI direction and does not introduce shadcn or a second auth route."
    - "Signed-out users still have a visible browser-level connectivity proof on `/auth` after `/` becomes protected."
  artifacts:
    - path: "src/routes/auth-page.tsx"
      provides: "Single auth route orchestrating sign-in, create-account, reset-request, reset-complete modes, and the signed-out connectivity proof"
      contains: "AuthPage"
    - path: "src/app/layout/AppFrame.tsx"
      provides: "Authenticated shell header with quiet sign-out control outside primary nav"
      contains: "Sign out"
    - path: "supabase/config.toml"
      provides: "Local auth redirect settings aligned to Netlify dev and in-app recovery"
      contains: "127.0.0.1:8888"
  key_links:
    - from: "src/routes/auth-page.tsx"
      to: "src/lib/auth/auth-state.ts"
      via: "auth actions and recovery-session awareness"
      pattern: "useAuth"
    - from: "src/routes/auth-page.tsx"
      to: "supabase/config.toml"
      via: "matching reset redirect target back into `/auth`"
      pattern: "redirectTo"
    - from: "src/routes/auth-page.tsx"
      to: "/household"
      via: "successful sign-in/sign-up navigation"
      pattern: "navigate\\('/household'\\)"
    - from: "src/routes/auth-page.tsx"
      to: "src/hooks/use-ping-status.ts"
      via: "signed-out connectivity proof reused after `/` becomes protected"
      pattern: "usePingStatus"
    - from: "src/app/layout/AppFrame.tsx"
      to: "src/lib/auth/auth-state.ts"
      via: "shell-level logout path that clears session and returns to `/auth`"
      pattern: "signOut"
---

<objective>
Implement the single-route auth experience for sign-up, sign-in, and in-app password recovery.

Purpose: Deliver the user-facing auth flows that sit on top of the foundation from Plan 01 while honoring the locked `/auth` and redirect decisions.
Output: A mode-switched `/auth` page, auth-page-local form states, validation/copy helpers, local Supabase redirect config aligned to Netlify dev, and shell-level logout wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-UI-SPEC.md
@src/routes/auth-page.tsx
@src/app/router.tsx
@src/app/layout/AppFrame.tsx
@src/app/providers.tsx
@supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 1 experience — replace the auth placeholder with the approved single-surface sign-in and sign-up flow</name>
  <files>src/routes/auth-page.tsx, src/lib/auth/auth-copy.ts, src/lib/auth/validation.ts</files>
  <read_first>.planning/phases/02-authentication/02-CONTEXT.md, .planning/phases/02-authentication/02-UI-SPEC.md, src/routes/auth-page.tsx</read_first>
  <action>Do this stage before touching password reset or local Supabase redirect config so the first half of the plan proves signed-out connectivity and credential auth from the single `/auth` surface. Replace the placeholder `src/routes/auth-page.tsx` with the real Phase 2 auth surface. Keep `/auth` as the only top-level auth route per D-01, and implement in-place mode switching between `Sign in` and `Create account` per D-02 using the approved editorial card layout, typography, spacing, and copy rules from `02-UI-SPEC.md`. To keep scope contained, implement the primary form states directly inside `src/routes/auth-page.tsx` unless a helper becomes meaningfully reusable; do not fan the plan out into thin component files. Build the UI with the repo's existing hand-authored Tailwind approach only; do not add shadcn. Centralize field validation and copy helpers in `src/lib/auth/validation.ts` and `src/lib/auth/auth-copy.ts` so the page can show direct field-level errors, in-flight button states, and recoverable submit failures. Because Plan 01 makes `/` protected per D-07 through D-09, keep a small signed-out connectivity proof on `/auth` by reusing the existing ping hook or an extracted equivalent so the browser suite still has a repo-native Netlify-to-Supabase smoke path after the redirect change. On successful sign-in or sign-up, navigate to `/household` per D-04 and D-05.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/routes/auth-page.tsx` no longer renders placeholder copy
    - the auth UI exposes adjacent `Sign in` and `Create account` modes on the same route
    - the signed-out `/auth` surface still renders a visible connectivity proof tied to the Netlify-routed ping path
    - success paths from both modes navigate to `/household`
    - validation and UI copy are factored into reusable helpers rather than embedded ad hoc in JSX
    - primary auth UI scope stays contained to `src/routes/auth-page.tsx` plus shared copy/validation helpers instead of proliferating one-file components
  </acceptance_criteria>
  <done>The app has a real single-route auth page for sign-in and account creation that matches the approved UI contract.</done>
</task>

<task type="auto">
  <name>Task 2: Stage 2 recovery — complete in-app password reset, shell logout, and local redirect alignment</name>
  <files>supabase/config.toml, src/routes/auth-page.tsx, src/app/layout/AppFrame.tsx, src/lib/auth/auth-state.ts</files>
  <read_first>.planning/phases/02-authentication/02-CONTEXT.md, supabase/config.toml</read_first>
  <action>Start this stage only after Task 1 is complete and successful sign-in/sign-up can already reach `/household`. Implement the two-step password reset flow required by D-03 and D-13 through D-15 without creating a second top-level auth page. Add a reset-request mode reachable from the same `/auth` surface, and call `supabase.auth.resetPasswordForEmail` with a redirect target that returns to the app's Netlify-served `/auth` URL. Update `supabase/config.toml` so local auth email redirects point at `http://127.0.0.1:8888` and explicitly allow the `/auth` return URL needed by Netlify dev. On return from the email link, detect the recovery session in the existing auth-state/client bootstrap, switch the same auth surface into reset-complete mode, collect new password plus confirmation, and finish the change with `supabase.auth.updateUser`. In the same stage, add the D-11 shell-level logout control to `src/app/layout/AppFrame.tsx` as a quiet far-right utility action outside the primary nav, wired to the shared `signOut` path from `src/lib/auth/auth-state.ts`, so authenticated users can always return to `/auth`. Keep the entire happy path in-app; do not hand off to Supabase hosted UI or introduce `/reset-password` as a separate top-level route.</action>
  <verify>rg -n 'site_url|additional_redirect_urls|127\\.0\\.0\\.1:8888|resetPasswordForEmail|updateUser|recovery|Sign out|signOut' supabase/config.toml src/routes/auth-page.tsx src/app/layout/AppFrame.tsx src/lib/auth/auth-state.ts && npm run build</verify>
  <acceptance_criteria>
    - `supabase/config.toml` no longer leaves `site_url` pinned only to `127.0.0.1:3000`
    - password reset request is entered from `/auth` instead of a new top-level page
    - the recovery link returns the user to the app and shows an in-app new-password form
    - password update completes through Supabase JS from the same auth surface
    - `src/app/layout/AppFrame.tsx` contains a visible `Sign out` action outside the main nav pill list
    - logout uses the shared auth-state path and returns the browser to `/auth`
  </acceptance_criteria>
  <done>Password recovery is fully implemented inside the app, shell logout exists everywhere in the authenticated frame, and local redirect config matches the Netlify-based development flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `rg -n 'site_url|additional_redirect_urls|127\\.0\\.0\\.1:8888|resetPasswordForEmail|updateUser|recovery|Sign out|signOut' supabase/config.toml src/routes/auth-page.tsx src/app/layout/AppFrame.tsx src/lib/auth/auth-state.ts` confirms the reset flow, redirect wiring, and shell logout path
- [ ] `/auth` remains the only top-level auth route in `src/app/router.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can sign up, sign in, sign out, and complete password reset from the approved single-route auth surface and authenticated shell while local redirects remain compatible with Netlify dev
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
