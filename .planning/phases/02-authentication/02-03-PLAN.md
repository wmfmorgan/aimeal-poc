---
phase: 02-authentication
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified: [src/components/auth/ProtectedRoute.test.tsx, src/lib/auth/validation.test.ts, src/routes/auth-page.test.tsx, tests/e2e/auth-flow.spec.ts, tests/e2e/ping-smoke.spec.ts, playwright.config.ts, scripts/dev/verify-phase2-auth.sh]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
user_setup: ["Ensure local Supabase, Netlify, and Inbucket can run during end-to-end verification"]
must_haves:
  truths:
    - "Phase 2 has automated unit coverage for auth redirect rules, validation helpers, and auth-page state transitions."
    - "Phase 2 has end-to-end coverage for sign-up, login, logout, protected-route redirect, and password-reset happy path."
    - "The end-to-end reset test proves the app-owned recovery flow by following the email link back into the app instead of using hosted UI."
    - "The existing browser smoke contract is revised so `/` redirect behavior and the authenticated connectivity badge are both covered after route protection lands."
  artifacts:
    - path: "src/components/auth/ProtectedRoute.test.tsx"
      provides: "Unit tests for signed-in and signed-out route decisions through the real guard module"
      contains: "ProtectedRoute"
    - path: "src/routes/auth-page.test.tsx"
      provides: "Component-level tests for auth mode switching and submission states"
      contains: "AuthPage"
    - path: "tests/e2e/auth-flow.spec.ts"
      provides: "Browser coverage for the Phase 2 auth happy paths"
      contains: "password reset"
    - path: "tests/e2e/ping-smoke.spec.ts"
      provides: "Revised smoke coverage for protected `/` redirect behavior and the surviving Netlify connectivity path"
      contains: "ping smoke"
    - path: "scripts/dev/verify-phase2-auth.sh"
      provides: "Single-command local verification flow for build, unit tests, and auth E2E"
      contains: "netlify dev"
  key_links:
    - from: "tests/e2e/auth-flow.spec.ts"
      to: "http://127.0.0.1:54334"
      via: "local Inbucket password-reset email inspection"
      pattern: "54334"
    - from: "tests/e2e/auth-flow.spec.ts"
      to: "src/routes/auth-page.tsx"
      via: "browser assertions for `/auth` mode switching and recovery completion"
      pattern: "/auth"
    - from: "tests/e2e/ping-smoke.spec.ts"
      to: "src/routes/auth-page.tsx"
      via: "signed-out redirect assertion and `/auth` connectivity proof"
      pattern: "API Connected|API Error|/auth"
    - from: "scripts/dev/verify-phase2-auth.sh"
      to: "tests/e2e/auth-flow.spec.ts, tests/e2e/ping-smoke.spec.ts"
      via: "scripted Playwright run against Netlify dev"
      pattern: "auth flow|ping smoke"
---

<objective>
Add the automated verification harness for the Phase 2 authentication flows.

Purpose: Lock in the auth contract before later phases start depending on protected routes and authenticated state.
Output: Unit tests for auth helpers and UI state, a Playwright auth happy-path suite, a revised browser smoke contract for the protected app entrypoint, and a repeatable local verification script.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-UI-SPEC.md
@.planning/phases/02-authentication/02-01-PLAN.md
@.planning/phases/02-authentication/02-02-PLAN.md
@playwright.config.ts
@src/test/setup.ts
@tests/e2e/ping-smoke.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit coverage for guard decisions, validation, and single-route auth UI state</name>
  <files>src/components/auth/ProtectedRoute.test.tsx, src/lib/auth/validation.test.ts, src/routes/auth-page.test.tsx</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/02-authentication/02-01-PLAN.md, .planning/phases/02-authentication/02-02-PLAN.md</read_first>
  <action>Create focused Vitest coverage for the actual guard module from Plan 01, the auth validation helpers, and the `/auth` page's mode-switching behavior. Because Plan 01 keeps redirect logic inside `src/components/auth/ProtectedRoute.tsx` instead of a separate helper file, add targeted tests around the guard's signed-in vs signed-out redirect outcomes rather than reintroducing another source module just for testability. Keep `src/routes/auth-page.test.tsx` centered on the single-surface page from Plan 02: sign-in vs create-account mode changes, reset entry and completion modes, and recoverable submit/loading states. If `src/routes/auth-page.tsx` becomes difficult to test directly, extract small pure helpers from the page through public modules without fanning the production plan back out into thin components or snapshots.</action>
  <verify>npm run test:unit -- --run src/components/auth/ProtectedRoute.test.tsx src/lib/auth/validation.test.ts src/routes/auth-page.test.tsx</verify>
  <acceptance_criteria>
    - `src/components/auth/ProtectedRoute.test.tsx` covers signed-out protected-route redirects and signed-in `/auth` redirects
    - `src/lib/auth/validation.test.ts` covers email/password validation and reset-password confirmation rules
    - `src/routes/auth-page.test.tsx` covers in-place auth mode changes without creating a second route
  </acceptance_criteria>
  <done>The core auth decision logic and single-surface UI behavior are protected by fast unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright coverage for sign-up, login persistence, logout, protected-route redirects, and in-app recovery</name>
  <files>tests/e2e/auth-flow.spec.ts, tests/e2e/ping-smoke.spec.ts, playwright.config.ts</files>
  <read_first>.planning/ROADMAP.md, supabase/config.toml, tests/e2e/ping-smoke.spec.ts</read_first>
  <action>Create `tests/e2e/auth-flow.spec.ts` as the Phase 2 browser contract and revise the existing `tests/e2e/ping-smoke.spec.ts` so Phase 1's smoke suite stays valid after `/` becomes protected. Use unique email addresses per run so the local auth database does not need cleanup between tests. In `auth-flow.spec.ts`, cover these flows explicitly: signed-out visit to `/household` redirects to `/auth`; sign-up succeeds and lands on `/household`; login persists across a browser close/reopen or a fresh browser context that reuses stored state per AUTH-02; logout from the authenticated shell returns to `/auth`; password reset request sends an email; the test retrieves the latest reset email from local Inbucket on `http://127.0.0.1:54334`, follows the link back into the app, completes the in-app password form, and confirms the user can sign in with the new password. In `ping-smoke.spec.ts`, replace the old signed-out `/` expectation with a two-part smoke: assert that a signed-out visit to `/` lands on `/auth`, then prove the connectivity badge still works through Netlify on the Phase 2 surface that now owns it, or after a lightweight authenticated hop if the badge intentionally remains behind auth. Keep both tests pointed at Netlify on `http://127.0.0.1:8888`; do not bypass the app by hitting Supabase auth endpoints directly.</action>
  <verify>npm run test:e2e -- --project=chromium --grep 'auth flow|ping smoke'</verify>
  <acceptance_criteria>
    - `tests/e2e/auth-flow.spec.ts` covers sign-up, login persistence, logout, protected-route redirect, and password reset
    - `tests/e2e/ping-smoke.spec.ts` no longer expects signed-out `/` to render the home badge directly
    - the smoke suite still proves a real Netlify-routed connectivity path after auth guards are introduced
    - the reset flow checks the local Inbucket email rather than assuming a mocked token
    - Playwright continues to target the Netlify-served app on port `8888`
  </acceptance_criteria>
  <done>Phase 2 has browser-level evidence for every required auth happy path.</done>
</task>

<task type="auto">
  <name>Task 3: Script the one-command Phase 2 auth verification run</name>
  <files>scripts/dev/verify-phase2-auth.sh, tests/e2e/auth-flow.spec.ts, tests/e2e/ping-smoke.spec.ts, playwright.config.ts</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, scripts/dev/verify-phase1-stack.sh</read_first>
  <action>Create `scripts/dev/verify-phase2-auth.sh` so execute-phase can validate the whole auth stack from a clean terminal. The script should run `npm run build`, `npm run test:unit -- --run src/components/auth/ProtectedRoute.test.tsx src/lib/auth/validation.test.ts src/routes/auth-page.test.tsx`, ensure the local Supabase stack is running, launch `netlify dev` on port `8888`, wait for readiness, run `npm run test:e2e -- --project=chromium --grep 'auth flow|ping smoke'`, and clean up the background Netlify process on exit. Reuse the cleanup style from `scripts/dev/verify-phase1-stack.sh` instead of inventing a second process-management approach so the updated smoke contract remains part of the single-command verification path.</action>
  <verify>bash scripts/dev/verify-phase2-auth.sh</verify>
  <acceptance_criteria>
    - `scripts/dev/verify-phase2-auth.sh` exists and runs build, focused unit tests, the auth Playwright suite, and the revised ping smoke coverage
    - the script starts from repo root and assumes the same Netlify/Supabase local topology as Phase 1
    - the script cleans up the background Netlify process even on failure
  </acceptance_criteria>
  <done>Execute-phase has a concrete single-command verification path for the full authentication phase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- --run src/components/auth/ProtectedRoute.test.tsx src/lib/auth/validation.test.ts src/routes/auth-page.test.tsx` passes
- [ ] `npm run test:e2e -- --project=chromium --grep 'auth flow|ping smoke'` passes against local Supabase + Netlify + Inbucket
- [ ] `bash scripts/dev/verify-phase2-auth.sh` passes from repo root
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has automated evidence for all four auth requirements, the protected-route redirect contract for `/`, and the router/session rules later phases depend on
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
