---
phase: 09-compact-meal-card-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/components/generation/MealPlanGrid.tsx
  - src/routes/plan-page.tsx
  - src/routes/plan-page.test.tsx
autonomous: true
requirements:
  - CARD-01
  - CARD-03
  - CARD-04

must_haves:
  truths:
    - "MealPlanGrid passes showMealTypeLabel={false} to every MealCard render"
    - "MealPlanGrid passes onCardClick to every MealCard render (replacing onViewDetails)"
    - "MealPlanGrid no longer passes favoriteStateByMealId, favoriteHelperTextByMealId, onSaveFavorite, or onOpenFavorites to MealCard"
    - "plan-page.tsx flyoutTrigger state is typed HTMLElement | null (not HTMLButtonElement)"
    - "plan-page.tsx handleViewDetails accepts HTMLElement trigger (not HTMLButtonElement)"
    - "MealPlanGrid call in plan-page.tsx no longer passes favorite-related props to MealPlanGrid"
    - "MealDetailFlyout in plan-page.tsx still receives all favorite props"
    - "A new plan-page test confirms card onClick fires flyout open"
  artifacts:
    - path: "src/components/generation/MealPlanGrid.tsx"
      provides: "Updated grid that wires onCardClick and showMealTypeLabel, removes favorite prop pass-through to cards"
    - path: "src/routes/plan-page.tsx"
      provides: "Updated route with broadened flyoutTrigger type and cleaned-up MealPlanGrid props"
    - path: "src/routes/plan-page.test.tsx"
      provides: "New test: card onClick triggers flyout open via onViewDetails wiring"
  key_links:
    - from: "MealPlanGrid renderSlot 'filled' case"
      to: "MealCard onCardClick"
      via: "onCardClick={(trigger) => onViewDetails?.(slot.slotKey, trigger)}"
      pattern: "onCardClick.*onViewDetails"
    - from: "plan-page.tsx handleViewDetails"
      to: "setFlyoutTrigger"
      via: "setFlyoutTrigger(trigger) with HTMLElement type"
      pattern: "HTMLElement"
    - from: "plan-page.tsx MealPlanGrid call"
      to: "MealDetailFlyout"
      via: "favorite props removed from grid, remain on flyout"
      pattern: "favoriteStateByMealId.*flyout"
---

<objective>
Wire the compact MealCard (produced in Plan 01) into MealPlanGrid and plan-page.tsx. Remove favorite prop pass-through from the grid layer. Broaden flyoutTrigger type to HTMLElement. Update plan-page.test.tsx with a card-click-opens-flyout test.

Purpose: Plan 01 changed MealCard's props. This plan updates every call site so TypeScript compiles and the card-to-flyout flow works end to end. No component rendering changes — only prop wiring and type updates.

Output: MealPlanGrid and plan-page.tsx fully wired to the new compact card interface. plan-page test confirms flyout opens on card click.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-compact-meal-card-refactor/09-CONTEXT.md
@.planning/phases/09-compact-meal-card-refactor/09-VALIDATION.md
@.planning/phases/09-compact-meal-card-refactor/09-01-SUMMARY.md

<interfaces>
<!-- MealCard NEW prop interface (produced by Plan 01): -->
type MealCardProps = {
  slot: Extract<MealPlanSlot, { state: "filled" }>;
  errorMessage?: string | null;
  isSelectionMode?: boolean;
  isSelected?: boolean;
  isEnriching?: boolean;
  isDeleting?: boolean;
  isFinalized?: boolean;
  showMealTypeLabel?: boolean;
  onDelete: () => void;
  onRegenerate: () => void;
  onRetryEnrichment?: () => void;
  onToggleSelection?: () => void;
  onCardClick?: (trigger: HTMLElement) => void;
};

<!-- MealPlanGrid CURRENT prop interface (to be updated): -->
// src/components/generation/MealPlanGrid.tsx lines 11-29
type MealPlanGridProps = {
  numDays: number;
  mealTypes: string[];
  slots: Record<string, MealSlot | MealPlanSlot>;
  isSelectionMode?: boolean;
  isFinalized?: boolean;
  selectedMealIds?: string[];
  pendingMealIds?: Record<string, boolean>;
  enrichmentErrorsByMealId?: Record<string, string | null>;
  favoriteStateByMealId?: Record<string, "disabled" | "ready" | "saved">;    // REMOVE
  favoriteHelperTextByMealId?: Record<string, string | null>;                  // REMOVE
  onDelete?: (slotKey: string) => void;
  onRegenerate?: (slotKey: string) => void;
  onRetryEnrichment?: (mealId: string) => void;
  onToggleSelectMeal?: (mealId: string) => void;
  onViewDetails?: (slotKey: string, trigger: HTMLButtonElement) => void;  // CHANGE trigger to HTMLElement
  onSaveFavorite?: (mealId: string) => void;                              // REMOVE
  onOpenFavorites?: (trigger: HTMLButtonElement) => void;                 // REMOVE
};

<!-- plan-page.tsx key state lines (to be updated): -->
// Line 46: const [flyoutTrigger, setFlyoutTrigger] = useState<HTMLButtonElement | null>(null);
// Line 182: function handleViewDetails(slotKey: string, trigger: HTMLButtonElement) {

<!-- plan-page.tsx MealPlanGrid call (current props to remove): -->
// Lines 325-341: favoriteStateByMealId, favoriteHelperTextByMealId, onSaveFavorite, onOpenFavorites
// NOTE: Keep these props on MealDetailFlyout call at lines 343-378 — they do NOT move off the flyout

<!-- plan-page.test.tsx existing MealPlanGrid mock (lines 113-117): -->
vi.mock("@/components/generation/MealPlanGrid", () => ({
  MealPlanGrid: ({ slots }: { slots: Record<string, unknown> }) => (
    <div data-testid="meal-plan-grid">{Object.keys(slots).join(",")}</div>
  ),
}));
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MealPlanGrid.tsx — remove favorite props, wire onCardClick</name>
  <read_first>
    - src/components/generation/MealPlanGrid.tsx (full file — read before edits)
    - .planning/phases/09-compact-meal-card-refactor/09-PATTERNS.md (MealPlanGrid section, lines 149-198)
  </read_first>
  <files>src/components/generation/MealPlanGrid.tsx</files>
  <action>
Read the full current MealPlanGrid.tsx file before making any changes.

**Props interface changes (MealPlanGridProps):**
- REMOVE: `favoriteStateByMealId?: Record<string, "disabled" | "ready" | "saved">`
- REMOVE: `favoriteHelperTextByMealId?: Record<string, string | null>`
- REMOVE: `onSaveFavorite?: (mealId: string) => void`
- REMOVE: `onOpenFavorites?: (trigger: HTMLButtonElement) => void`
- CHANGE: `onViewDetails?: (slotKey: string, trigger: HTMLButtonElement) => void` → `onViewDetails?: (slotKey: string, trigger: HTMLElement) => void`

**Destructured params — remove the same four props from the function signature:**
Remove `favoriteStateByMealId = {}`, `favoriteHelperTextByMealId = {}`, `onSaveFavorite`, `onOpenFavorites` from the destructured parameter list.

**renderSlot "filled" case (lines 105-124) — update MealCard call:**
REMOVE these props from the MealCard call:
- `favoriteState={favoriteStateByMealId[slot.meal.id] ?? "disabled"}`
- `favoriteHelperText={favoriteHelperTextByMealId[slot.meal.id] ?? null}`
- `onSaveFavorite={() => onSaveFavorite?.(slot.meal.id)}`
- `onOpenFavorites={onOpenFavorites}`
- `onViewDetails={onViewDetails ? (trigger) => onViewDetails(slot.slotKey, trigger) : undefined}`

ADD these props to the MealCard call:
- `showMealTypeLabel={false}`
- `onCardClick={onViewDetails ? (trigger) => onViewDetails(slot.slotKey, trigger) : undefined}`

**renderSlot "error" case (lines 149-170) — update MealCard call similarly:**
REMOVE the same favorite-related props from this MealCard call too.
REMOVE `onViewDetails` callback.
ADD `showMealTypeLabel={false}` and `onCardClick={onViewDetails ? (trigger) => onViewDetails(slot.slotKey, trigger) : undefined}`.

**Non-managed slot fallback (lines 85-99) — update:**
This `<MealCard>` call only has `slot`, `onDelete`, `onRegenerate` — it does not pass favorite or view detail props. No change needed here, but add `showMealTypeLabel={false}` for consistency (it is in the grid context).

After edits, run `npx tsc --noEmit` to verify TypeScript compiles on this file.
  </action>
  <verify>
    <automated>npx vitest run src/components/generation/meal-plan-management.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "favoriteStateByMealId\|favoriteHelperTextByMealId\|onSaveFavorite\|onOpenFavorites" src/components/generation/MealPlanGrid.tsx` returns 0 lines
    - `grep -n "onViewDetails" src/components/generation/MealPlanGrid.tsx` returns only the prop type declaration line and one internal usage in the renderSlot function — NOT passed to MealCard
    - `grep -n "onCardClick" src/components/generation/MealPlanGrid.tsx` returns at least 2 lines (passed to MealCard in filled and error cases)
    - `grep -n "showMealTypeLabel={false}" src/components/generation/MealPlanGrid.tsx` returns at least 2 lines
    - `npx vitest run src/components/generation/meal-plan-management.test.tsx` exits 0
  </acceptance_criteria>
  <done>MealPlanGrid.tsx passes all existing tests. Favorite props removed from grid. MealCard receives showMealTypeLabel={false} and onCardClick in all call sites. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update plan-page.tsx flyout wiring and plan-page.test.tsx card-click test</name>
  <read_first>
    - src/routes/plan-page.tsx (lines 40-55 for state types, lines 180-190 for handleViewDetails, lines 314-345 for MealPlanGrid call)
    - src/routes/plan-page.test.tsx (full file — understand existing mock structure before adding test)
    - .planning/phases/09-compact-meal-card-refactor/09-PATTERNS.md (plan-page section lines 264-311, plan-page test section lines 461-503)
  </read_first>
  <files>
    src/routes/plan-page.tsx
    src/routes/plan-page.test.tsx
  </files>
  <action>
Read the relevant sections of plan-page.tsx before editing.

**plan-page.tsx changes:**

1. **Broaden flyoutTrigger state type (line 46):**
   Change: `const [flyoutTrigger, setFlyoutTrigger] = useState<HTMLButtonElement | null>(null);`
   To: `const [flyoutTrigger, setFlyoutTrigger] = useState<HTMLElement | null>(null);`

2. **Broaden handleViewDetails trigger type (line 182):**
   Change: `function handleViewDetails(slotKey: string, trigger: HTMLButtonElement) {`
   To: `function handleViewDetails(slotKey: string, trigger: HTMLElement) {`
   The function body (`setSelectedSlotKey(slotKey); setFlyoutTrigger(trigger);`) stays unchanged.

3. **Remove favorite props from MealPlanGrid call (lines 325-341):**
   In the `<MealPlanGrid ... />` JSX, remove these four props:
   - `favoriteStateByMealId={favoriteStateByMealId}`
   - `favoriteHelperTextByMealId={favoriteHelperTextByMealId}`
   - `onSaveFavorite={(mealId) => { void handleSaveFavorite(mealId); }}`
   - `onOpenFavorites={(trigger) => { setOverlayTrigger(trigger); setIsFavoritesOpen(true); }}`

   IMPORTANT: These four props must REMAIN on the `<MealDetailFlyout ... />` call at lines 343-378. Do NOT remove them from the flyout. Only remove them from the MealPlanGrid call.

4. **MealDetailFlyout returnFocusTo prop type compatibility:**
   The flyout already types `returnFocusTo` as `HTMLElement | null` (verified in MealDetailFlyout.tsx line 7). The `flyoutTrigger` change to `HTMLElement | null` makes this compatible without any cast. No change needed to the flyout call site.

**plan-page.test.tsx changes:**

Read the full test file before editing. Note the existing `vi.mock("@/components/generation/MealPlanGrid", ...)` stub.

Update the MealPlanGrid mock to expose `onViewDetails` so the test can invoke it:
```tsx
vi.mock("@/components/generation/MealPlanGrid", () => ({
  MealPlanGrid: ({ onViewDetails }: { onViewDetails?: (slotKey: string, trigger: HTMLElement) => void }) => (
    <div data-testid="meal-plan-grid">
      <button
        type="button"
        onClick={(e) => onViewDetails?.("Monday:dinner", e.currentTarget)}
      >
        Open card flyout
      </button>
    </div>
  ),
}));
```

Then add a new test (in the PersistedPlanView describe block, or at the top level if tests aren't nested):

```tsx
it("card click wires through onViewDetails to open the flyout", async () => {
  // The mock plan needs a filled slot at Monday:dinner
  // Find or create the appropriate mockUseMealPlan return value that has a plan with meals
  // Render the plan page with a valid plan ID
  // Click "Open card flyout"
  // Wait for the flyout dialog to appear

  renderPlanPage("plan-xyz");  // use whatever helper already exists in plan-page.test.tsx

  fireEvent.click(screen.getByRole("button", { name: "Open card flyout" }));

  await waitFor(() => {
    expect(screen.getByRole("dialog")).toBeInTheDocument();
  });
});
```

Read the existing test file carefully to find: (1) what mock data structure the tests use for a loaded plan with meals, (2) what render helper (renderPlanPage or similar) is used, (3) which describe block to add the new test inside. Match the existing test patterns exactly.

If the existing tests use a plan mock that has no filled slots, the flyout won't open because `managedSlots["Monday:dinner"]?.state !== "filled"`. Ensure the mock plan has at least one meal entry at "Monday:dinner" matching the slotKey the updated MealPlanGrid stub fires.
  </action>
  <verify>
    <automated>npx vitest run src/routes/plan-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "HTMLButtonElement" src/routes/plan-page.tsx` returns 0 lines in the flyoutTrigger state declaration and handleViewDetails signature (only HTMLElement should remain in those locations)
    - `grep -n "favoriteStateByMealId\|favoriteHelperTextByMealId\|onSaveFavorite\|onOpenFavorites" src/routes/plan-page.tsx` — these props MUST still exist in the MealDetailFlyout call, but MUST NOT exist in the MealPlanGrid call. Run: `grep -A5 "MealPlanGrid" src/routes/plan-page.tsx | grep -c "favoriteStateByMealId"` must return 0.
    - `grep -n "onViewDetails.*Monday:dinner" src/routes/plan-page.test.tsx` returns at least 1 line (new stub exposes the callback)
    - `grep -n "Open card flyout" src/routes/plan-page.test.tsx` returns at least 1 line (new stub button)
    - `grep -n "getByRole.*dialog" src/routes/plan-page.test.tsx` returns at least 1 line (new flyout-open assertion)
    - `npx vitest run src/routes/plan-page.test.tsx` exits 0 with all tests passing
  </acceptance_criteria>
  <done>plan-page.tsx compiles with HTMLElement trigger type. MealPlanGrid call no longer passes favorite props. MealDetailFlyout still receives favorite props. New plan-page test confirms card click triggers flyout open. All plan-page tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure prop wiring change — no new input surfaces, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | plan-page.tsx flyoutTrigger type broadening | accept | Broadening HTMLButtonElement to HTMLElement does not relax any security boundary; it is a TypeScript type adjustment for accessibility (focus return). The underlying element is still set by trusted internal event handlers. |
| T-09-05 | Denial | Favorite props removed from MealPlanGrid | accept | Favorite data still flows to MealDetailFlyout; favorite save/open functionality is unchanged. Removing from grid only removes the UI affordance from card surface, which is the intended behavior (D-11, D-18). |
</threat_model>

<verification>
**Per-task automated verification (from VALIDATION.md):**
- Task 1 (CARD-01, CARD-03): `npx vitest run src/components/generation/meal-plan-management.test.tsx`
- Task 2 (CARD-04): `npx vitest run src/routes/plan-page.test.tsx`
- Full wave check: `npx vitest run src/components/generation/ src/routes/plan-page.test.tsx`
- TypeScript compilation: `npx tsc --noEmit`
</verification>

<success_criteria>
- MealPlanGrid.tsx TypeScript compiles: no errors referencing removed props
- MealPlanGrid passes showMealTypeLabel={false} and onCardClick to every MealCard render
- plan-page.tsx flyoutTrigger and handleViewDetails use HTMLElement (not HTMLButtonElement)
- MealPlanGrid call in plan-page.tsx has no favorite-related props
- MealDetailFlyout call in plan-page.tsx retains all favorite-related props
- plan-page.test.tsx new test: card click fires onViewDetails → flyout dialog appears
- Full vitest suite remains at 155+ passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-compact-meal-card-refactor/09-02-SUMMARY.md`
</output>
