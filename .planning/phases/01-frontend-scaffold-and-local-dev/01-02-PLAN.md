---
phase: 01-frontend-scaffold-and-local-dev
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/main.tsx, src/app/providers.tsx, src/app/router.tsx, src/app/layout/AppFrame.tsx, src/routes/home-page.tsx, src/routes/auth-page.tsx, src/routes/household-page.tsx, src/routes/plan-page.tsx, src/lib/trpc/client.ts, src/hooks/use-ping-status.ts]
autonomous: true
requirements: [DEPL-01]
user_setup: ["Run local Supabase services before netlify dev verification"]
must_haves:
  truths:
    - "The app boots through a real React router with all four Phase 1 routes present."
    - "The home page shows a visible success/error state driven by a live tRPC ping request through the Netlify proxy."
    - "The frontend uses relative `/functions/v1/...` calls so local and future hosted environments share the same API path shape."
  artifacts:
    - path: "src/app/router.tsx"
      provides: "Phase 1 route scaffold for home, auth, household, and plan surfaces"
      contains: "/plan/:id"
    - path: "src/lib/trpc/client.ts"
      provides: "Frontend tRPC client pointed at the Netlify proxy path"
      contains: "/functions/v1/trpc"
    - path: "src/hooks/use-ping-status.ts"
      provides: "Reusable ping query state for the home connectivity badge"
      contains: "ping"
    - path: "src/routes/home-page.tsx"
      provides: "Visible API connected/error badge on the home route"
      contains: "API Connected"
  key_links:
    - from: "src/app/providers.tsx"
      to: "src/hooks/use-ping-status.ts"
      via: "TanStack Query client provider"
      pattern: "QueryClient"
    - from: "src/app/router.tsx"
      to: "src/routes/home-page.tsx"
      via: "route registration for `/`"
      pattern: "path:\\s*['\"]/"
    - from: "src/routes/home-page.tsx"
      to: "src/lib/trpc/client.ts"
      via: "ping status hook"
      pattern: "usePingStatus"
---

<objective>
Wire the React application, placeholder routes, and the visible ping smoke test against the existing tRPC edge function.

Purpose: Prove the frontend can talk to the current backend through Netlify and establish the route skeleton later phases build on.
Output: App entrypoint, providers, router, four route pages, a tRPC client module, and a home-page connectivity badge.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md
@architecture.md
@supabase/functions/trpc/index.ts
@.claude/skills/spike-findings-aimeal-poc/references/edge-functions-ai.md
@.planning/phases/01-frontend-scaffold-and-local-dev/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the React entrypoint, providers, and route scaffold</name>
  <files>src/main.tsx, src/app/providers.tsx, src/app/router.tsx, src/app/layout/AppFrame.tsx, src/routes/home-page.tsx, src/routes/auth-page.tsx, src/routes/household-page.tsx, src/routes/plan-page.tsx</files>
  <read_first>.planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md, architecture.md</read_first>
  <action>Create the app entrypoint and router using React Router. Add route definitions for `/`, `/auth`, `/household`, and `/plan/:id`, all wrapped in a shared editorial `AppFrame` layout. Keep every route open with no guards. The non-home routes should be clear placeholder pages naming the future surface they represent so later phases can fill them in without replacing the routing structure. Import `src/styles/globals.css` from `src/main.tsx`.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/app/router.tsx` defines routes for `/`, `/auth`, `/household`, and `/plan/:id`
    - `src/main.tsx` mounts the router and imports `src/styles/globals.css`
    - the placeholder pages contain human-readable labels for Auth, Household, and Plan surfaces
  </acceptance_criteria>
  <done>The React app boots with the correct Phase 1 route structure and shared layout.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the frontend tRPC client and reusable ping query hook</name>
  <files>src/app/providers.tsx, src/lib/trpc/client.ts, src/hooks/use-ping-status.ts</files>
  <read_first>supabase/functions/trpc/index.ts, .claude/skills/spike-findings-aimeal-poc/references/edge-functions-ai.md, .planning/ROADMAP.md</read_first>
  <action>Create `src/lib/trpc/client.ts` that points to the relative endpoint `/functions/v1/trpc` and uses the HTTP transport supported by the frontend package choice from Plan 01. Add `src/app/providers.tsx` to instantiate and export a shared `QueryClient` wrapper for the app. Add `src/hooks/use-ping-status.ts` that performs the `ping` query and normalizes the response into `connected`, `error`, and `updatedAt` UI state. Reuse the existing backend `ping` procedure as-is; do not modify `supabase/functions/trpc/index.ts` in this phase.</action>
  <verify>rg -n '/functions/v1/trpc|ping' src/lib/trpc/client.ts src/hooks/use-ping-status.ts && npm run build</verify>
  <acceptance_criteria>
    - `src/lib/trpc/client.ts` references `/functions/v1/trpc`
    - `src/hooks/use-ping-status.ts` invokes the `ping` procedure
    - `src/app/providers.tsx` creates a TanStack Query client used by the router tree
  </acceptance_criteria>
  <done>The frontend can issue the Phase 1 connectivity call through the Netlify proxy path.</done>
</task>

<task type="auto">
  <name>Task 3: Render the visible API connectivity badge on the home route</name>
  <files>src/routes/home-page.tsx</files>
  <read_first>src/hooks/use-ping-status.ts, .planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md</read_first>
  <action>Implement the home page as a lightweight editorial landing view for local development. Display a visible badge or chip that renders `API Connected` when the ping succeeds and `API Error` when the query fails. Include enough supporting copy to tell a developer that the app is talking to the local Supabase/tRPC stack through Netlify. Keep the page intentionally small; do not add a `/dev` page or auth flow here.</action>
  <verify>rg -n 'API Connected|API Error' src/routes/home-page.tsx && npm run build</verify>
  <acceptance_criteria>
    - `src/routes/home-page.tsx` contains the exact strings `API Connected` and `API Error`
    - the home page consumes `usePingStatus`
    - the page copy references the local Netlify-to-Supabase connection
  </acceptance_criteria>
  <done>The home route provides an immediate visual proof of backend connectivity for Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `rg -n '/functions/v1/trpc|ping' src/lib/trpc/client.ts src/hooks/use-ping-status.ts` confirms the live ping path
- [ ] `rg -n 'API Connected|API Error' src/routes/home-page.tsx` confirms the visible smoke-test badge text
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app boots with the required Phase 1 routes and a visible ping status on the home page
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-scaffold-and-local-dev/01-02-SUMMARY.md`
</output>
