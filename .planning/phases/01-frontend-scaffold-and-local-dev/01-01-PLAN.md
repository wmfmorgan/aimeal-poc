---
phase: 01-frontend-scaffold-and-local-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json, vite.config.ts, netlify.toml, index.html, tsconfig.json, tsconfig.app.json, tsconfig.node.json, tailwind.config.ts, postcss.config.js, src/styles/globals.css, scripts/dev/serve-trpc-local.sh]
autonomous: true
requirements: [DEPL-01, DEPL-02]
user_setup: ["Ensure Docker Desktop is running before Supabase commands", "Populate supabase/functions/.env with required API keys before end-to-end verification"]
must_haves:
  truths:
    - "The repository contains a Vite + React 19 + TypeScript frontend that Netlify can start through npm scripts."
    - "The frontend styling foundation reflects the locked editorial theme choices from Phase 1 context."
    - "Local dev configuration points frontend traffic through netlify dev instead of direct per-service URLs."
  artifacts:
    - path: "package.json"
      provides: "Frontend scripts including Vite entrypoint and local function serve helper"
      contains: "\"dev:trpc\""
    - path: "netlify.toml"
      provides: "Netlify local dev bridge and Supabase proxy redirects"
      contains: "127.0.0.1:54331"
    - path: "src/styles/globals.css"
      provides: "Editorial design tokens and typography defaults"
      contains: "#4A6741"
    - path: "scripts/dev/serve-trpc-local.sh"
      provides: "Checked-in local function serve command that loads supabase/functions/.env"
      contains: "--env-file supabase/functions/.env"
  key_links:
    - from: "netlify.toml"
      to: "vite.config.ts"
      via: "matching dev target port configuration"
      pattern: "targetPort = 5173"
    - from: "package.json"
      to: "scripts/dev/serve-trpc-local.sh"
      via: "npm script wrapper"
      pattern: "dev:trpc"
    - from: "index.html"
      to: "src/styles/globals.css"
      via: "font loading plus global theme token import"
      pattern: "Newsreader|Manrope"
---

<objective>
Bootstrap the frontend toolchain and local-dev bridge required for every later phase.

Purpose: Establish a stable React/Tailwind/Netlify foundation so later phases can add features without revisiting local setup.
Output: Frontend package/config files, Netlify proxy configuration, and global design tokens aligned to the editorial cookbook direction.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md
@architecture.md
@supabase/config.toml
@.claude/skills/spike-findings-aimeal-poc/references/local-dev-infrastructure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the frontend package and Vite/Tailwind toolchain</name>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, tailwind.config.ts, postcss.config.js</files>
  <read_first>.planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md, architecture.md, .claude/skills/spike-findings-aimeal-poc/references/local-dev-infrastructure.md</read_first>
  <action>Create a Vite + React 19 + TypeScript frontend in the repo root using npm. Define scripts `dev`, `build`, `preview`, `lint`, `test:unit`, `test:e2e`, and `dev:trpc`. Add the runtime packages needed for Phase 1 only: `react`, `react-dom`, `react-router-dom`, `@tanstack/react-query`, `@trpc/client`, `superjson`, and the styling/tooling packages for Tailwind. Configure `vite.config.ts` to serve on port `5173` so Netlify can target it. Keep the setup compatible with later shadcn/ui adoption by enabling Tailwind content scanning over `index.html` and `src/**/*.{ts,tsx}`.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `package.json` contains scripts named `dev`, `build`, `lint`, `test:unit`, `test:e2e`, and `dev:trpc`
    - `vite.config.ts` configures the dev server to use port `5173`
    - `tailwind.config.ts` scans `./index.html` and `./src/**/*.{ts,tsx}`
  </acceptance_criteria>
  <done>The repo can install and build a React 19 frontend with the expected scripts and Vite/Tailwind configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Add Netlify dev proxy configuration and checked-in env-file serve helper</name>
  <files>netlify.toml, scripts/dev/serve-trpc-local.sh, package.json</files>
  <read_first>supabase/config.toml, .claude/skills/spike-findings-aimeal-poc/references/local-dev-infrastructure.md, .planning/ROADMAP.md</read_first>
  <action>Create `netlify.toml` with a `[dev]` block that runs `npm run dev`, serves Netlify on port `8888`, and targets Vite on port `5173`. Add three `[[redirects]]` entries with `status = 200` and `force = true` for `/functions/v1/*`, `/rest/v1/*`, and `/auth/v1/*`, each pointing to `http://127.0.0.1:54331/...` using `:splat`. Also create `scripts/dev/serve-trpc-local.sh` and wire it to the `dev:trpc` npm script so local function serving always uses `supabase functions serve trpc --env-file supabase/functions/.env --no-verify-jwt` with no manual `export` step. Match the already-configured Supabase API port from `supabase/config.toml`; do not introduce alternate ports or conditional env switching.</action>
  <verify>rg -n 'targetPort = 5173|from = \"/functions/v1/\\*\"|from = \"/rest/v1/\\*\"|from = \"/auth/v1/\\*\"|force = true|127\\.0\\.0\\.1:54331|dev:trpc|--env-file supabase/functions/.env|--no-verify-jwt' netlify.toml package.json scripts/dev/serve-trpc-local.sh && bash scripts/dev/serve-trpc-local.sh >/tmp/phase1-trpc.log 2>&1 & TRPC_PID=$!; sleep 5; ps -p $TRPC_PID -o command= | rg -- '--env-file supabase/functions/.env'; kill $TRPC_PID</verify>
  <acceptance_criteria>
    - `netlify.toml` includes a `[dev]` block with `port = 8888` and `targetPort = 5173`
    - `netlify.toml` includes redirects for `/functions/v1/*`, `/rest/v1/*`, and `/auth/v1/*`
    - every redirect target uses `http://127.0.0.1:54331`
    - `scripts/dev/serve-trpc-local.sh` runs `supabase functions serve trpc --env-file supabase/functions/.env --no-verify-jwt`
  </acceptance_criteria>
  <done>Netlify is configured as the single local entry point and the repo contains an executable local serve helper that loads `supabase/functions/.env` without manual exports.</done>
</task>

<task type="auto">
  <name>Task 3: Install the editorial theme foundation in the app shell assets</name>
  <files>index.html, src/styles/globals.css</files>
  <read_first>.planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md, .planning/PROJECT.md</read_first>
  <action>Update `index.html` to load Newsreader and Manrope from Google Fonts CDN. Create `src/styles/globals.css` with Tailwind layers or equivalent global CSS that defines the warm off-white surface `#faf9f8`, sage green primary `#4A6741`, typography defaults, spacing-first layout treatment, and no 1px border-heavy framing. Expose the core color tokens as CSS custom properties so later plans and phases can reuse them without redefining the design system.</action>
  <verify>rg -n 'Newsreader|Manrope|4A6741|faf9f8|--color' index.html src/styles/globals.css</verify>
  <acceptance_criteria>
    - `index.html` includes font loading for `Newsreader` and `Manrope`
    - `src/styles/globals.css` contains the exact color values `#4A6741` and `#faf9f8`
    - `src/styles/globals.css` defines reusable CSS custom properties for the theme
  </acceptance_criteria>
  <done>The frontend has the locked editorial visual foundation required by the phase context.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `rg -n '127\\.0\\.0\\.1:54331|force = true|dev:trpc|--env-file supabase/functions/.env' netlify.toml package.json scripts/dev/serve-trpc-local.sh` confirms proxy and env-file wiring
- [ ] `rg -n 'Newsreader|Manrope|4A6741|faf9f8' index.html src/styles/globals.css` confirms theme wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo contains the frontend toolchain and Netlify proxy foundation required for Phase 1 execution
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-scaffold-and-local-dev/01-01-SUMMARY.md`
</output>
