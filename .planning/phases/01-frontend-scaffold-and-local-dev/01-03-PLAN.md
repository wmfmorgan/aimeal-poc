---
phase: 01-frontend-scaffold-and-local-dev
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified: [vitest.config.ts, src/test/setup.ts, src/hooks/use-ping-status.test.ts, playwright.config.ts, tests/e2e/ping-smoke.spec.ts, scripts/dev/verify-phase1-stack.sh]
autonomous: true
requirements: [DEPL-01, DEPL-02]
user_setup: ["Ensure supabase/functions/.env contains valid keys or harmless placeholders before running Supabase and Netlify locally"]
must_haves:
  truths:
    - "Phase 1 has automated coverage for both frontend bootstrap logic and the Netlify-to-Supabase ping path."
    - "The local verification flow exercises the app through Netlify rather than bypassing it."
    - "The local verification recipe includes an executable env-file-backed function serve step and a reproducible Netlify smoke run."
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration for the frontend source tree"
      contains: "test"
    - path: "src/hooks/use-ping-status.test.ts"
      provides: "Unit test coverage for Phase 1 ping logic"
      contains: "describe"
    - path: "playwright.config.ts"
      provides: "Playwright config targeting the Netlify-served app"
      contains: "127.0.0.1:8888"
    - path: "tests/e2e/ping-smoke.spec.ts"
      provides: "Smoke test for the home route connectivity badge"
      contains: "API Connected"
    - path: "scripts/dev/verify-phase1-stack.sh"
      provides: "Scripted clean-terminal verification for build, local services, and smoke coverage"
      contains: "netlify dev"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/e2e/ping-smoke.spec.ts"
      via: "Playwright baseURL configuration"
      pattern: "127.0.0.1:8888"
    - from: "src/hooks/use-ping-status.test.ts"
      to: "src/hooks/use-ping-status.ts"
      via: "direct module import"
      pattern: "use-ping-status"
    - from: "scripts/dev/verify-phase1-stack.sh"
      to: "tests/e2e/ping-smoke.spec.ts"
      via: "scripted smoke invocation"
      pattern: "ping smoke"
---

<objective>
Add the automated verification harness for Phase 1 so execute-phase can prove the local dev stack works end-to-end.

Purpose: Lock in the frontend bootstrap and the Netlify proxy behavior with repeatable tests before later phases add complexity.
Output: Vitest and Playwright config plus one focused unit test and one smoke-level end-to-end test.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-scaffold-and-local-dev/01-CONTEXT.md
@.planning/phases/01-frontend-scaffold-and-local-dev/01-01-PLAN.md
@.planning/phases/01-frontend-scaffold-and-local-dev/01-02-PLAN.md
@supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure frontend unit testing for the Phase 1 app shell</name>
  <files>vitest.config.ts, src/test/setup.ts, src/hooks/use-ping-status.test.ts</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-frontend-scaffold-and-local-dev/01-02-PLAN.md</read_first>
  <action>Create a Vitest configuration for the React frontend, including a shared test setup file. Add one focused unit test for the Phase 1 ping-status logic in `src/hooks/use-ping-status.test.ts`. The test should assert behavior that would fail if the hook/client stopped calling the ping path or stopped surfacing success/error state correctly; do not write a snapshot-only test. If a hook-level test becomes awkward, extract a small pure helper from the hook and test that helper through the public module.</action>
  <verify>npm run test:unit -- --run</verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists and points at the frontend source tree
    - `src/test/setup.ts` exists and is referenced by the Vitest config
    - `src/hooks/use-ping-status.test.ts` asserts success and failure behavior for the ping logic
  </acceptance_criteria>
  <done>The Phase 1 frontend logic has a real unit-test harness and a meaningful ping-path test.</done>
</task>

<task type="auto">
  <name>Task 2: Add the netlify-dev smoke test for the end-to-end ping flow</name>
  <files>playwright.config.ts, tests/e2e/ping-smoke.spec.ts</files>
  <read_first>.planning/ROADMAP.md, supabase/config.toml, .claude/skills/spike-findings-aimeal-poc/references/local-dev-infrastructure.md</read_first>
  <action>Create a Playwright config that targets `http://127.0.0.1:8888` and define a smoke test in `tests/e2e/ping-smoke.spec.ts`. The test should load the home route served by `netlify dev` and assert that the visible connectivity badge reaches the connected state when the local Supabase stack is running. Keep the test scope narrow: app boot, route render, and successful ping through Netlify to the existing edge function. Do not bypass Netlify by pointing Playwright directly at Vite or Supabase ports.</action>
  <verify>npm run test:e2e -- --project=chromium --grep 'ping smoke'</verify>
  <acceptance_criteria>
    - `playwright.config.ts` targets port `8888`
    - `tests/e2e/ping-smoke.spec.ts` visits `/` and checks for the connected badge text
    - the smoke test interacts only with the Netlify-served app entrypoint
  </acceptance_criteria>
  <done>Phase 1 has end-to-end coverage for the Netlify proxy plus tRPC ping integration path.</done>
</task>

<task type="auto">
  <name>Task 3: Script the clean-terminal verification flow for local Phase 1 checks</name>
  <files>scripts/dev/verify-phase1-stack.sh, playwright.config.ts, tests/e2e/ping-smoke.spec.ts, vitest.config.ts, src/test/setup.ts, src/hooks/use-ping-status.test.ts</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `scripts/dev/verify-phase1-stack.sh` so execute-phase can prove the Phase 1 flow from a clean terminal. The script should run `npm run build`, `npm run test:unit -- --run`, start `supabase start` if the local stack is not already running, launch `netlify dev` in the background on port `8888`, wait for readiness, run `npm run test:e2e -- --project=chromium --grep 'ping smoke'`, and clean up the background Netlify process on exit. In the summary, record the exact command and note that `supabase/functions/.env` is the only required secrets file.</action>
  <verify>bash scripts/dev/verify-phase1-stack.sh</verify>
  <acceptance_criteria>
    - `scripts/dev/verify-phase1-stack.sh` exists and launches `netlify dev` before running the Playwright smoke test
    - the smoke test assumes Netlify on `8888` and the Supabase API on `54331`
    - the summary can point to a repeatable single-command verification flow from repo root
  </acceptance_criteria>
  <done>Execute-phase has a concrete, repeatable verification recipe for the entire Phase 1 stack.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `npm run test:unit -- --run` passes
- [ ] `bash scripts/dev/verify-phase1-stack.sh` passes against the local Netlify + Supabase stack
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has automated evidence for frontend boot, ping logic, and Netlify-routed smoke coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-scaffold-and-local-dev/01-03-SUMMARY.md`
</output>
