---
phase: 06-enrichment-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260422000002_spoonacular_usage.sql
  - src/lib/generation/types.ts
  - src/lib/generation/spoonacular-enrichment.ts
  - src/lib/generation/spoonacular-enrichment.test.ts
  - src/lib/generation/spoonacular-usage.ts
  - src/lib/generation/spoonacular-usage.test.ts
  - src/hooks/use-meal-enrichment.test.ts
  - src/routes/dev-page.test.tsx
  - tests/e2e/enrichment-flow.spec.ts
autonomous: true
requirements:
  - ENRCH-02
  - ENRCH-03
  - DEVT-02
must_haves:
  truths:
    - "The database can persist per-call Spoonacular usage events and aggregate them by UTC day."
    - "Shared enrichment types cover both draft and enriched meal payloads without replacing the Phase 5 persisted-plan route model."
    - "Wave 1-3 work has test files and pure helper contracts to build against instead of inventing them ad hoc."
    - "The plan explicitly records the cache-retention and concurrency-policy conflict instead of hiding it in implementation."
  artifacts:
    - path: "supabase/migrations/20260422000002_spoonacular_usage.sql"
      provides: "Spoonacular usage-log table, indexes, and RLS policies"
      contains: "create table spoonacular_usage"
    - path: "src/lib/generation/spoonacular-enrichment.ts"
      provides: "Pure cache/transform helpers for Spoonacular recipe payloads"
      exports: ["buildEnrichedMealPatch", "isRecipeCached", "getInstructionSteps"]
    - path: "src/lib/generation/spoonacular-usage.ts"
      provides: "Usage-event normalization and UTC summary helpers"
      exports: ["buildUsageEvent", "summarizeUsageByUtcDay"]
    - path: "src/lib/generation/types.ts"
      provides: "Shared enriched meal and usage-report contracts"
      exports: ["PersistedMeal", "SpoonacularUsageEntry", "SpoonacularUsageSummary"]
    - path: "tests/e2e/enrichment-flow.spec.ts"
      provides: "Wave 0 end-to-end scaffold for enrichment flow"
  key_links:
    - from: "supabase/migrations/20260422000002_spoonacular_usage.sql"
      to: "src/lib/generation/spoonacular-usage.ts"
      via: "shared UTC-day and per-call usage contract"
      pattern: "usage_date_utc|quota_used"
    - from: "src/lib/generation/types.ts"
      to: "src/components/generation/MealDetailFlyout.tsx"
      via: "enriched meal fields consumed by the flyout"
      pattern: "ingredients|nutrition|instructions|image_url"
    - from: "src/lib/generation/spoonacular-enrichment.test.ts"
      to: "src/lib/generation/spoonacular-enrichment.ts"
      via: "pure helper contract tests"
      pattern: "buildEnrichedMealPatch|isRecipeCached"
---

<objective>
Wave 0 foundation: add the schema and shared contracts for enrichment usage tracking, extend persisted meal types for recipe data, and create the helper/test scaffolding the remaining Phase 6 plans depend on.

Purpose: Phase 6 needs one blocking schema change for usage logging plus stable enrichment contracts before the server path, batch hook, and UI can be implemented without guesswork.
Output: Usage migration, DB push gate, shared enrichment/usage helpers, extended types, and Wave 0 test scaffolds.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-enrichment-flow/06-CONTEXT.md
@.planning/phases/06-enrichment-flow/06-RESEARCH.md
@.planning/phases/06-enrichment-flow/06-UI-SPEC.md
@.planning/phases/06-enrichment-flow/06-VALIDATION.md
@.planning/phases/05-meal-plan-grid-and-management/05-04-PLAN.md
@architecture.md

<interfaces>
From src/lib/generation/types.ts:
```typescript
export type PersistedMeal = {
  id: string;
  day_of_week: DayOfWeek;
  meal_type: MealType;
  title: string;
  short_description: string;
  rationale: string | null;
  status: "draft" | "enriched";
};
```

From supabase/migrations/20260419000001_initial_schema.sql:
```sql
create table meals (
  id uuid primary key default gen_random_uuid(),
  meal_plan_id uuid references meal_plans on delete cascade,
  spoonacular_recipe_id bigint,
  ingredients jsonb,
  nutrition jsonb,
  instructions text[],
  image_url text
);

create table spoonacular_cache (
  spoonacular_recipe_id bigint primary key,
  title text,
  ingredients jsonb,
  nutrition jsonb,
  instructions text[],
  image_url text,
  cached_at timestamptz default now()
);
```

From src/routes/dev-page.tsx:
```tsx
export function DevPage() {
  const { logs, isLoading, error } = useLlmLogs();
  return (
    <>
      <section>LLM Requests</section>
      <section>Spoonacular Usage</section>
    </>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [BLOCKING] Add Spoonacular usage migration and push local schema</name>
  <files>
    supabase/migrations/20260422000002_spoonacular_usage.sql
  </files>
  <read_first>
    - supabase/migrations/20260419000001_initial_schema.sql
    - .planning/phases/06-enrichment-flow/06-RESEARCH.md
    - .planning/phases/06-enrichment-flow/06-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `supabase/migrations/20260422000002_spoonacular_usage.sql` creates `spoonacular_usage`.
    - The table stores `meal_id`, `meal_plan_id`, `spoonacular_recipe_id`, `cache_hit`, `endpoint`, `points_used`, `quota_request`, `quota_used`, `quota_left`, `usage_date_utc`, and `created_at`.
    - RLS permits authenticated reads scoped to the caller's owned meal plans and service-role inserts for logging.
    - The migration adds an index on `usage_date_utc, created_at desc` for `/dev` reads.
    - The Wave 0 summary records the explicit Phase 6 decision that longer-lived Spoonacular cache reuse remains active for the PoC as conscious risk acceptance.
    - `supabase db push` is executed as part of this task before any Wave 1 work proceeds.
  </acceptance_criteria>
  <action>
Create `supabase/migrations/20260422000002_spoonacular_usage.sql` to introduce a dedicated usage-log table for `DEVT-02`/`DEVT-04`. The schema must support both summary and per-call reporting, so include the meal linkage, whether the call was a cache hit or miss, the Spoonacular endpoint label, the measured point cost and quota-header snapshot, and a stored UTC usage day. Follow the repo's existing migration + RLS style rather than inventing a parallel logging mechanism.

Make this task the Wave 0 gate: after the migration file is written, run `supabase db push` and verify the table exists locally. Mark the implementation summary with the explicit resolved Phase 6 decision that the project is still honoring long-lived cache-first reuse per D-10 and PROJECT.md for the PoC, while treating the current Spoonacular pricing-page one-hour language as documented risk acceptance. Do not silently add a hidden expiry policy in this task.
  </action>
  <verify>
    <automated>supabase db push && supabase db query "select table_name from information_schema.tables where table_name = 'spoonacular_usage';" | grep -q spoonacular_usage</automated>
  </verify>
  <done>
The local database contains a queryable Spoonacular usage-log table, and Phase 6 has an explicit blocking schema checkpoint plus a recorded cache-policy decision before server or UI work starts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend enrichment contracts and create Wave 0 helper/test scaffolds</name>
  <files>
    src/lib/generation/types.ts
    src/lib/generation/spoonacular-enrichment.ts
    src/lib/generation/spoonacular-enrichment.test.ts
    src/lib/generation/spoonacular-usage.ts
    src/lib/generation/spoonacular-usage.test.ts
    src/hooks/use-meal-enrichment.test.ts
    src/routes/dev-page.test.tsx
    tests/e2e/enrichment-flow.spec.ts
  </files>
  <read_first>
    - src/lib/generation/types.ts
    - src/hooks/use-meal-plan.ts
    - src/components/generation/MealDetailFlyout.tsx
    - src/routes/dev-page.tsx
    - .planning/phases/06-enrichment-flow/06-VALIDATION.md
    - .planning/phases/06-enrichment-flow/06-UI-SPEC.md
  </read_first>
  <behavior>
    - `PersistedMeal` gains nullable enriched-recipe fields instead of replacing the Phase 5 draft contract.
    - `src/lib/generation/spoonacular-enrichment.ts` contains pure helpers for cache freshness, recipe-field extraction, and meal-patch assembly.
    - `src/lib/generation/spoonacular-usage.ts` contains pure helpers for usage-event shaping and UTC-day summarization.
    - Wave 0 tests exist for transform helpers, usage summarization, batch-hook behavior, dev-page rendering, and E2E flow names listed in `06-VALIDATION.md`.
  </behavior>
  <action>
Extend `src/lib/generation/types.ts` so the shared persisted meal contract can carry recipe fields in-place for `mealPlan.get`, the enriched flyout, and live card updates. Add nullable `spoonacular_recipe_id`, `ingredients`, `nutrition`, `instructions`, and `image_url` fields to `PersistedMeal`, plus explicit `SpoonacularUsageEntry` and `SpoonacularUsageSummary` types for the `/dev` surface.

Create `src/lib/generation/spoonacular-enrichment.ts` and `src/lib/generation/spoonacular-usage.ts` as the pure helper layer the edge function/router and client tests will share. Write real unit coverage for those helpers in `src/lib/generation/spoonacular-enrichment.test.ts` and `src/lib/generation/spoonacular-usage.test.ts`. Also create Wave 0 scaffolds named exactly as the validation contract expects for `src/hooks/use-meal-enrichment.test.ts`, `src/routes/dev-page.test.tsx`, and `tests/e2e/enrichment-flow.spec.ts` so later plans can fill them in without inventing new filenames. Keep the scaffolds executable in CI, using `test.todo` or minimal red-to-green placeholders rather than ad hoc notes in markdown.
  </action>
  <verify>
    <automated>npx vitest run src/lib/generation/spoonacular-enrichment.test.ts src/lib/generation/spoonacular-usage.test.ts</automated>
  </verify>
  <done>
Shared enrichment and usage contracts are defined, the flyout/query layer has stable types to consume, and Wave 1-3 execution can build against named test files instead of creating them late.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge function logging -> Postgres | Server-generated Spoonacular usage events cross into persisted cost-accounting data |
| shared helper contracts -> UI/API layers | Incorrect recipe-field or usage-summary helpers would propagate bad data across both the grid and `/dev` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `spoonacular_usage` migration | mitigate | Store immutable per-call rows with UTC day and quota-header snapshots so `/dev` derives from persisted facts instead of mutable counters |
| T-06-02 | I | usage-log RLS | mitigate | Scope authenticated reads through owned meal plans and keep inserts privileged so one user cannot inspect another user's quota activity |
| T-06-03 | T | enrichment field helpers | mitigate | Lock recipe-field extraction and usage summarization behind unit tests before router/UI code depends on them |
| T-06-04 | R | Spoonacular cache-retention policy | accept | Keep cache-first reuse per D-10 and project docs for the PoC, but document the current pricing-page one-hour language as an explicit risk/assumption in code comments and the plan summary |
</threat_model>

<verification>
- `supabase db push`
- `supabase db query "select table_name from information_schema.tables where table_name = 'spoonacular_usage';"`
- `npx vitest run src/lib/generation/spoonacular-enrichment.test.ts src/lib/generation/spoonacular-usage.test.ts`
</verification>

<success_criteria>
- Phase 6 has a real schema-backed usage-log table before backend or UI work begins.
- Persisted meal types can represent both draft and enriched states without changing the Phase 5 route/flyout model.
- The cache-policy conflict is documented as a conscious PoC risk, not ignored.
</success_criteria>

<output>
After completion, create `.planning/phases/06-enrichment-flow/06-01-SUMMARY.md`
</output>
