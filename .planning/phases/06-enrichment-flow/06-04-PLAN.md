---
phase: 06-enrichment-flow
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - src/hooks/use-meal-enrichment.test.ts
  - src/components/generation/meal-detail-flyout.test.tsx
  - src/routes/dev-page.test.tsx
  - tests/e2e/enrichment-flow.spec.ts
autonomous: false
requirements:
  - ENRCH-01
  - ENRCH-02
  - ENRCH-03
  - ENRCH-04
  - ENRCH-05
  - DEVT-02
  - DEVT-04
must_haves:
  truths:
    - "The enrichment flow is regression-covered across batch selection, live card updates, cached reopen, retry, flyout rendering, and `/dev` reporting."
    - "Live Spoonacular verification proves the app can enrich real meals and record usage data with a real API key."
    - "Partial failures remain card-local in both automated and manual verification."
  artifacts:
    - path: "tests/e2e/enrichment-flow.spec.ts"
      provides: "End-to-end enrichment regression suite"
    - path: "src/hooks/use-meal-enrichment.test.ts"
      provides: "Hook-level retry and partial-failure coverage"
    - path: "src/routes/dev-page.test.tsx"
      provides: "Usage-report rendering assertions"
  key_links:
    - from: "tests/e2e/enrichment-flow.spec.ts"
      to: "src/routes/plan-page.tsx"
      via: "select/enrich/retry/flyout interaction flow"
      pattern: "Enrich selected meals|Retry enrichment"
    - from: "tests/e2e/enrichment-flow.spec.ts"
      to: "src/routes/dev-page.tsx"
      via: "usage summary and per-call breakdown verification"
      pattern: "Spoonacular Usage"
---

<objective>
Close Phase 6 with regression coverage, explicit failure/retry validation, and the unavoidable live-human verification step for third-party recipe data and quota accounting.

Purpose: The enrichment path crosses UI, DB, and an external API, so the phase is not done until both automated coverage and a real-key verification prove the route, flyout, and `/dev` diagnostics work together.
Output: Green unit/E2E coverage plus a blocking human verification checkpoint with exact live test steps.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-enrichment-flow/06-CONTEXT.md
@.planning/phases/06-enrichment-flow/06-VALIDATION.md
@.planning/phases/06-enrichment-flow/06-03-PLAN.md
@src/routes/plan-page.tsx
@src/routes/dev-page.tsx
@tests/e2e
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish unit and end-to-end coverage for selection, retry, cache reuse, flyout, and `/dev`</name>
  <files>
    src/hooks/use-meal-enrichment.test.ts
    src/components/generation/meal-detail-flyout.test.tsx
    src/routes/dev-page.test.tsx
    tests/e2e/enrichment-flow.spec.ts
  </files>
  <read_first>
    - src/hooks/use-meal-enrichment.test.ts
    - src/components/generation/meal-detail-flyout.test.tsx
    - src/routes/dev-page.test.tsx
    - tests/e2e/generation-flow.spec.ts
    - .planning/phases/06-enrichment-flow/06-VALIDATION.md
  </read_first>
  <behavior>
    - The hook tests cover local progress, partial failure isolation, unsafe-match local errors, and `retryMeal`.
    - The E2E spec covers selection mode, `Select all`, batch submit, live enriched-card updates, flyout rendering, cache-hit reopen, and `/dev` usage totals.
    - Failure coverage proves one card can fail while sibling cards complete successfully, including an unsafe Spoonacular-match rejection path.
  </behavior>
  <action>
Complete the missing coverage listed in `06-VALIDATION.md` instead of creating a new validation shape. Extend the hook and component tests so they assert per-card retry, enriched flyout rendering, and the real `/dev` usage presentation. Then build `tests/e2e/enrichment-flow.spec.ts` to cover the full route path: open an existing plan, enter selection mode, select meals, run enrichment, verify cards update live, open an enriched flyout, confirm cache-hit behavior on a second open or repeated enrichment, and inspect `/dev` for daily totals and per-call rows.

Include explicit automated scenarios for local failure handling. One scenario should force a single meal enrichment failure while sibling meals succeed, then assert the failed card shows `Retry enrichment` and the successful cards remain enriched. A second scenario should force an allergy or avoidance mismatch on Spoonacular's best match and assert the write is blocked, the failed card stays local, and retry still targets only that card. Keep the test suite aligned with the existing route and fixture style from earlier phases rather than inventing a new test harness.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/use-meal-enrichment.test.ts src/components/generation/meal-detail-flyout.test.tsx src/routes/dev-page.test.tsx && npx playwright test tests/e2e/enrichment-flow.spec.ts</automated>
  </verify>
  <done>
Automated coverage proves the Phase 6 flow works end-to-end, including cache reuse, partial failure isolation, flyout recipe rendering, and usage reporting.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Spoonacular verification with a real API key</name>
  <what-built>
    The full enrichment flow is implemented on the persisted `/plan/:id` route, the flyout renders real recipe data, and `/dev` reports Spoonacular usage totals and recent calls.
  </what-built>
  <how-to-verify>
    1. Start the local stack with a valid Spoonacular API key available to the Supabase edge function.
    2. Open an existing populated plan at `/plan/:id` and confirm the Phase 5 grid/flyout shell still behaves normally before enrichment.
    3. Enter `Select meals`, choose at least two draft meals, and run `Enrich selected meals`.
    4. Confirm each selected card shows local progress, at least one card flips to `Enriched` before the whole batch finishes, and any induced failure stays local to its card.
    5. Open an enriched card and confirm the right-side flyout shows image, ingredients, instructions, nutrition, then the original rationale.
    6. Retry one failed card if applicable and confirm the retry does not restart already-enriched siblings.
    7. Open `/dev` and confirm today's points used, requests made, cache hits, cache misses, and the recent per-call breakdown reflect the observed activity.
    8. Repeat enrichment on an already-enriched or cached meal and confirm the app reuses cached data without creating a duplicate live Spoonacular fetch path.
    9. Note whether the observed cache behavior is being operated as a PoC risk acceptance relative to the pricing-page one-hour cache language documented in `06-RESEARCH.md`.
  </how-to-verify>
  <resume-signal>Type "approved" if the live flow behaves as expected, or describe the failing step(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated test fixtures -> enrichment route | Test doubles must accurately represent mixed success, cache-hit, and live-call paths |
| live verification -> third-party API behavior | Real Spoonacular responses and quota headers can vary and must be confirmed with a live key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | R | regression coverage | mitigate | Encode batch selection, partial failure, cache reuse, flyout rendering, and `/dev` reporting as executable unit/E2E tests, not prose-only checks |
| T-06-14 | D | live API variance | mitigate | Require one blocking human verification pass with a real Spoonacular key before the phase is considered complete |
| T-06-15 | R | pricing/cache-policy conflict | accept | During human verification, explicitly record whether the implementation is operating under PoC risk acceptance for cache retention rather than silently treating the policy as settled |
</threat_model>

<verification>
- `npx vitest run src/hooks/use-meal-enrichment.test.ts src/components/generation/meal-detail-flyout.test.tsx src/routes/dev-page.test.tsx`
- `npx playwright test tests/e2e/enrichment-flow.spec.ts`
- Human verification checklist above with a real Spoonacular key
</verification>

<success_criteria>
- Automated coverage exists for cache-first enrichment, retry/failure behavior, enriched flyout content, and `/dev` usage reporting.
- A real-key manual pass confirms the route, flyout, and `/dev` surfaces behave correctly against live Spoonacular responses.
- The cache-retention conflict remains visible as a documented assumption/risk at sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/06-enrichment-flow/06-04-SUMMARY.md`
</output>

<source_audit>
## Multi-Source Coverage Audit

### GOAL Coverage

| Source Item | Covered By |
|-------------|------------|
| Select draft meals and enrich them from the persisted plan grid | `06-02` Task 2, `06-03` Task 1, `06-04` Task 1 |
| Fetch real Spoonacular recipe data with cache-first lookup | `06-02` Task 1, `06-04` Task 1 |
| View full recipe data in the existing right-side flyout | `06-03` Task 2, `06-04` Task 1 |
| Show Spoonacular spend and usage breakdown on `/dev` | `06-01` Task 1, `06-02` Task 1, `06-03` Task 2, `06-04` Task 1 |

### REQ Coverage

| Requirement | Coverage |
|-------------|----------|
| ENRCH-01 | `06-02` Task 2, `06-03` Task 1, `06-04` Task 1 |
| ENRCH-02 | `06-01` Task 2, `06-02` Task 1, `06-04` Task 1 |
| ENRCH-03 | `06-01` Task 2, `06-02` Task 1, `06-04` Task 1 |
| ENRCH-04 | `06-02` Task 2, `06-03` Task 1, `06-04` Task 1 |
| ENRCH-05 | `06-03` Task 2, `06-04` Task 1 |
| DEVT-02 | `06-01` Task 1, `06-02` Task 1, `06-04` Task 1 |
| DEVT-04 | `06-02` Task 1, `06-03` Task 2, `06-04` Task 1 |

### RESEARCH Coverage

| Research Item | Coverage |
|---------------|----------|
| Cache-first enrichment with shared cache writes kept privileged | `06-02` Task 1 |
| Client-fanned single-meal mutations for live per-card progress | `06-02` Task 2, `06-03` Task 1 |
| Extend `mealPlan.get`/persisted types instead of a separate recipe route | `06-01` Task 2, `06-03` Task 2 |
| Usage reporting derived from persisted rows and UTC-day grouping | `06-01` Task 1, `06-02` Task 1, `06-03` Task 2 |
| Pricing/cache-policy conflict must be surfaced | `06-01` Task 1, `06-02` Task 1, `06-04` Task 2 |

### CONTEXT Coverage

| Decision | Coverage |
|----------|----------|
| D-01, D-02, D-03, D-04 | `06-03` Task 1, `06-04` Task 1 |
| D-05, D-06, D-07, D-08 | `06-02` Task 2, `06-03` Task 1, `06-04` Task 1 |
| D-09, D-10, D-11 | `06-02` Task 1 |
| D-12, D-13, D-14, D-15 | `06-03` Task 2, `06-04` Task 1 |
| D-16, D-17, D-18 | `06-01` Task 1, `06-02` Task 1, `06-03` Task 2 |
| D-19, D-20 | `06-01` Task 2, `06-03` Task 1, `06-03` Task 2 |
</source_audit>
