---
phase: 05-meal-plan-grid-and-management
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/app/layout/AppFrame.tsx
  - src/routes/plan-page.tsx
  - src/routes/plan-page.test.tsx
autonomous: true
requirements:
  - PLAN-01
must_haves:
  truths:
    - "Clicking the main Plan nav opens the newest saved plan when one exists."
    - "Refreshing or deep-linking to `/plan/:id` loads the persisted plan instead of the generation-only state."
    - "A persisted plan page shows a clear `Create new plan` action without hiding the current plan."
  artifacts:
    - path: "src/app/layout/AppFrame.tsx"
      provides: "Latest-plan navigation resolution"
      exports: ["AppFrame"]
    - path: "src/routes/plan-page.tsx"
      provides: "Route orchestration for new-plan generation mode and persisted-plan management mode"
      exports: ["PlanPage"]
    - path: "src/routes/plan-page.test.tsx"
      provides: "Route-level persisted plan tests"
  key_links:
    - from: "src/app/layout/AppFrame.tsx"
      to: "src/hooks/use-meal-plan.ts"
      via: "latest plan lookup before navigation"
      pattern: "useLatestMealPlan"
    - from: "src/routes/plan-page.tsx"
      to: "src/lib/generation/plan-slots.ts"
      via: "persisted slot normalization before rendering MealPlanGrid"
      pattern: "buildMealPlanSlots"
---

<objective>
Turn navigation and route orchestration into a persisted plan-management surface by resolving the newest saved plan from the nav and teaching `PlanPage` to load either generation mode or saved-plan mode.

Purpose: Phase 5 is not complete until revisits to `/plan/:id` hydrate from the database and the user can intentionally start a new plan from that persisted view.
Output: Updated AppFrame nav behavior, persisted `PlanPage` route mode, `Create new plan` action, and route tests.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-meal-plan-grid-and-management/05-CONTEXT.md
@.planning/phases/05-meal-plan-grid-and-management/05-RESEARCH.md
@.planning/phases/05-meal-plan-grid-and-management/05-UI-SPEC.md
@.planning/phases/05-meal-plan-grid-and-management/05-VALIDATION.md
@.planning/phases/05-meal-plan-grid-and-management/05-01-PLAN.md

<interfaces>
From src/hooks/use-meal-plan.ts (Plan 01):
```typescript
export function useLatestMealPlan(): {
  latestMealPlan: { id: string } | null;
  isLoading: boolean;
  error: Error | null;
};

export function useMealPlan(planId: string): {
  mealPlan: PersistedMealPlan | null;
  isLoading: boolean;
  error: Error | null;
};
```

From src/lib/generation/plan-slots.ts (Plan 01):
```typescript
export function buildMealPlanSlots(plan: PersistedMealPlan): Record<string, MealPlanSlot>;
```

From src/routes/plan-page.tsx:
```typescript
const showForm = id === "new" || state === "idle";
const showGrid = state === "streaming" || state === "complete" || state === "error";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve the Plan nav to the newest saved plan</name>
  <files>
    src/app/layout/AppFrame.tsx
    src/routes/plan-page.test.tsx
  </files>
  <read_first>
    - src/app/layout/AppFrame.tsx
    - src/hooks/use-meal-plan.ts
    - src/app/router.tsx
    - .planning/phases/05-meal-plan-grid-and-management/05-CONTEXT.md
    - .planning/phases/05-meal-plan-grid-and-management/05-UI-SPEC.md
    - .planning/phases/05-meal-plan-grid-and-management/05-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/app/layout/AppFrame.tsx` no longer hard-codes `to: "/plan/new"` inside `navItems`.
    - `src/app/layout/AppFrame.tsx` contains a click handler that routes to `/plan/${id}` when `latestMealPlan?.id` exists and `/plan/new` otherwise.
    - `src/app/layout/AppFrame.tsx` preserves the existing nav pill styling classes.
    - `src/routes/plan-page.test.tsx` contains a test name matching `routes the Plan nav to the latest saved plan`.
  </acceptance_criteria>
  <action>
Update `AppFrame` for D-01 by removing the static `Plan` destination and handling it with programmatic navigation. Keep the current visual nav pill treatment, but make the `Plan` item trigger latest-plan resolution through the Plan 01 hook contract and navigate to `/plan/${latestId}` when present, otherwise `/plan/new`. Keep failure behavior conservative: if the latest-plan lookup errors, fall back to `/plan/new` rather than blocking the user.

Add route-level coverage in `src/routes/plan-page.test.tsx` for the latest-plan nav path so the behavior is pinned before the route rewrite lands.
  </action>
  <verify>
    <automated>npx vitest run src/routes/plan-page.test.tsx</automated>
  </verify>
  <done>
The main Plan nav opens the newest saved plan when available and falls back safely to `/plan/new`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite PlanPage for persisted revisit mode and intentional new-plan creation</name>
  <files>
    src/routes/plan-page.tsx
    src/routes/plan-page.test.tsx
  </files>
  <read_first>
    - src/routes/plan-page.tsx
    - src/hooks/use-generation-stream.ts
    - src/hooks/use-meal-plan.ts
    - src/lib/generation/plan-slots.ts
    - src/components/generation/MealPlanGrid.tsx
    - .planning/phases/05-meal-plan-grid-and-management/05-CONTEXT.md
    - .planning/phases/05-meal-plan-grid-and-management/05-UI-SPEC.md
    - .planning/phases/05-meal-plan-grid-and-management/05-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/routes/plan-page.tsx` branches persisted mode on `id !== "new"` and uses `useMealPlan(id)`.
    - `src/routes/plan-page.tsx` uses `buildMealPlanSlots` before rendering `MealPlanGrid`.
    - `src/routes/plan-page.tsx` renders the exact copy `Create new plan`.
    - `src/routes/plan-page.tsx` keeps `/plan/new` generation behavior intact by continuing to use `useGenerationStream`.
    - `src/routes/plan-page.test.tsx` contains test names matching `hydrates a persisted meal plan by route id` and `shows Create new plan on a persisted plan`.
  </acceptance_criteria>
  <action>
Rewrite `PlanPage` so route mode and data mode are separate. Keep `/plan/new` on the existing generation path from Phase 4, but for any real plan id use `useMealPlan(id)` from Plan 01, normalize the returned data with `buildMealPlanSlots`, and render the management shell instead of relying on in-memory stream state. Add a visible `Create new plan` action in the header card per D-03, and keep page-level loading and error banners separate from slot-local states that will arrive in later plans.

Do not introduce inline editing or edit-language copy. The persisted page heading, supporting copy, and CTA labels should align with the approved UI-SPEC management language and preserve the existing editorial shell.
  </action>
  <verify>
    <automated>npx vitest run src/routes/plan-page.test.tsx</automated>
  </verify>
  <done>
Refreshing or deep-linking to `/plan/:id` loads a persisted plan, while `/plan/new` still starts generation and the persisted view exposes `Create new plan`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser nav interaction -> route navigation | User-triggered navigation must not resolve to another user's plan id |
| route param -> persisted plan query | Untrusted `:id` parameters cross from the URL into authenticated reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I | `AppFrame` plan nav | mitigate | Resolve nav targets through `useLatestMealPlan` backed by authenticated tRPC reads instead of trusting local route strings |
| T-05-05 | T | `PlanPage` route param handling | mitigate | Treat `id === "new"` as the only generation path and pass all other ids through the validated `mealPlan.get` procedure |
| T-05-06 | D | persisted plan route | mitigate | Render page-level loading and retry states instead of crashing or silently falling back to generation on failed reads |
</threat_model>

<verification>
- `npx vitest run src/routes/plan-page.test.tsx`
- `rg -n 'Create new plan|useMealPlan\\(id\\)|buildMealPlanSlots' src/routes/plan-page.tsx`
- `rg -n 'latestMealPlan|/plan/new' src/app/layout/AppFrame.tsx`
</verification>

<success_criteria>
- The Plan nav resolves to the newest saved plan or `/plan/new` when none exists.
- `PlanPage` supports both `/plan/new` generation mode and persisted `/plan/:id` management mode.
- The persisted management page includes a visible `Create new plan` action without losing the current plan grid.
</success_criteria>

<output>
After completion, create `.planning/phases/05-meal-plan-grid-and-management/05-02-SUMMARY.md`
</output>
