---
phase: 05-meal-plan-grid-and-management
plan: 04
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - src/routes/plan-page.tsx
  - src/components/generation/MealCard.tsx
  - src/components/generation/MealPlanGrid.tsx
  - src/components/generation/MealDetailFlyout.tsx
  - src/components/generation/meal-detail-flyout.test.tsx
  - tests/e2e/plan-management.spec.ts
autonomous: true
requirements:
  - PLAN-02
  - PLAN-04
must_haves:
  truths:
    - "Clicking a meal opens a right-side flyout while the weekly grid stays visible behind it."
    - "Keyboard focus moves into the flyout, stays trapped there while open, and returns to the invoking control on close."
    - "Phase 5 exposes no inline title-edit affordance anywhere in the grid or flyout."
  artifacts:
    - path: "src/components/generation/MealDetailFlyout.tsx"
      provides: "Accessible right-side meal details flyout"
      exports: ["MealDetailFlyout"]
    - path: "src/components/generation/meal-detail-flyout.test.tsx"
      provides: "Focus-management component tests for the flyout"
    - path: "tests/e2e/plan-management.spec.ts"
      provides: "Persisted plan management end-to-end coverage"
  key_links:
    - from: "src/routes/plan-page.tsx"
      to: "src/components/generation/MealDetailFlyout.tsx"
      via: "selected slot state and open/close handlers"
      pattern: "MealDetailFlyout"
    - from: "tests/e2e/plan-management.spec.ts"
      to: "src/components/generation/MealCard.tsx"
      via: "assert presence of flyout trigger and absence of inline edit UI"
      pattern: "View details"
---

<objective>
Finish Phase 5 with the right-side flyout, accessibility semantics, and end-to-end regression coverage, including explicit traceability for the locked no-inline-edit decision.

Purpose: The detail view is the user-facing explanation layer for a generated meal, and Phase 5 needs an explicit guardrail so stale `PLAN-02` wording does not leak back into implementation.
Output: Route-integrated flyout, focus-management tests, Playwright management flow coverage, and a no-inline-edit regression contract.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-meal-plan-grid-and-management/05-CONTEXT.md
@.planning/phases/05-meal-plan-grid-and-management/05-RESEARCH.md
@.planning/phases/05-meal-plan-grid-and-management/05-UI-SPEC.md
@.planning/phases/05-meal-plan-grid-and-management/05-VALIDATION.md
@.planning/phases/05-meal-plan-grid-and-management/05-03-PLAN.md

<interfaces>
From src/components/generation/MealCard.tsx (Plan 03):
```tsx
type MealCardProps = {
  slot: MealPlanSlot;
  onViewDetails: (slotKey: string) => void;
  onDelete: (slotKey: string) => void;
  onRegenerate: (slotKey: string) => void;
};
```

From src/routes/plan-page.tsx:
```tsx
const slots = buildMealPlanSlots(mealPlan);
return <MealPlanGrid slots={slots} ... />;
```

From 05-UI-SPEC accessibility contract:
```text
- Flyout traps focus while open and returns focus to the originating card action on close.
- Close via explicit button, Escape, backdrop click, or route/state reset.
- Do not introduce inline editable text fields on cards.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the accessible right-side flyout and route-level selection state</name>
  <files>
    src/routes/plan-page.tsx
    src/components/generation/MealCard.tsx
    src/components/generation/MealPlanGrid.tsx
    src/components/generation/MealDetailFlyout.tsx
    src/components/generation/meal-detail-flyout.test.tsx
  </files>
  <read_first>
    - src/routes/plan-page.tsx
    - src/components/generation/MealCard.tsx
    - src/components/generation/MealPlanGrid.tsx
    - .planning/phases/05-meal-plan-grid-and-management/05-CONTEXT.md
    - .planning/phases/05-meal-plan-grid-and-management/05-RESEARCH.md
    - .planning/phases/05-meal-plan-grid-and-management/05-UI-SPEC.md
    - .planning/phases/05-meal-plan-grid-and-management/05-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `src/components/generation/MealDetailFlyout.tsx` exists and exports `MealDetailFlyout`.
    - `src/components/generation/MealDetailFlyout.tsx` contains the exact section heading `Why this fits`.
    - `src/components/generation/MealDetailFlyout.tsx` handles `Escape` and restores focus to the invoking control.
    - `src/routes/plan-page.tsx` stores selected-slot state and renders `MealDetailFlyout` alongside `MealPlanGrid`.
    - `src/components/generation/meal-detail-flyout.test.tsx` contains test names matching `traps focus while open` and `returns focus to the invoking button on close`.
  </acceptance_criteria>
  <action>
Add `MealDetailFlyout` as a bespoke right-side slide-over, not a centered modal and not inline expansion, per D-07 through D-09. Keep the grid visible beneath a translucent backdrop, structure the flyout so later enrichment sections can be appended without replacing the shell, and expose the required content blocks: meal title, day/meal context, short description, rationale under `Why this fits`, and management actions. Implement focus management as a dialog-like interaction shell: move focus inside on open, trap tab order while open, close on `Escape` and backdrop click, and restore focus to the invoking control on close.

Integrate flyout selection state in `PlanPage` rather than hiding it inside one card so route-level delete/regenerate updates and future enriched recipe data can share the same surface. `MealCard` and `MealPlanGrid` should pass a clear `View details` action into this route-level state. Do not add any editable text field, `contentEditable`, or title-input affordance.
  </action>
  <verify>
    <automated>npx vitest run src/components/generation/meal-detail-flyout.test.tsx</automated>
  </verify>
  <done>
The meal detail flyout opens from the grid, preserves spatial context, and meets the required keyboard/focus behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add end-to-end management coverage and explicit no-inline-edit regression checks</name>
  <files>
    tests/e2e/plan-management.spec.ts
    src/components/generation/meal-detail-flyout.test.tsx
    src/components/generation/MealCard.tsx
  </files>
  <read_first>
    - tests/e2e/generation-flow.spec.ts
    - src/components/generation/MealCard.tsx
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-meal-plan-grid-and-management/05-CONTEXT.md
    - .planning/phases/05-meal-plan-grid-and-management/05-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `tests/e2e/plan-management.spec.ts` exists and covers revisit, delete, regenerate, and flyout flows.
    - `tests/e2e/plan-management.spec.ts` contains a test or assertion label matching `no inline edit`.
    - `src/components/generation/MealCard.tsx` contains no `input`, `textarea`, `contentEditable`, `Edit title`, `Rename`, or `Save changes` strings.
    - `src/components/generation/meal-detail-flyout.test.tsx` or `tests/e2e/plan-management.spec.ts` asserts that no inline title-edit affordance is rendered, satisfying PLAN-02 traceability per D-16 and D-17.
  </acceptance_criteria>
  <action>
Create `tests/e2e/plan-management.spec.ts` to cover the persisted revisit flow, opening and closing the flyout, deleting a meal into an empty slot, regenerating from both a filled slot and an empty slot, and confirming sibling slots stay visible. Include an explicit regression test named around `no inline edit` that asserts the absence of inline title-edit affordances. This is how PLAN-02 stays represented in Phase 5 without violating the locked D-04 product decision.

Back that Playwright coverage with one focused component-level assertion in `meal-detail-flyout.test.tsx` or `MealCard`-adjacent coverage so the no-inline-edit rule fails fast in unit tests as well as E2E. Do not silently ignore the stale requirement text; encode the mismatch as a guarded negative contract.
  </action>
  <verify>
    <automated>npx vitest run src/components/generation/meal-detail-flyout.test.tsx && npx playwright test tests/e2e/plan-management.spec.ts</automated>
  </verify>
  <done>
The flyout and management flows are covered end-to-end, and Phase 5 has an explicit regression guard preventing inline title editing from slipping back in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/pointer interaction -> flyout state | Local UI state must remain accessible and not strand focus outside the active panel |
| regression suite -> stale requirement text | A stale requirements statement could reintroduce disallowed UI without an explicit guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | D | `MealDetailFlyout` focus handling | mitigate | Implement Escape close, focus trap, and focus return tests so keyboard users cannot get stranded |
| T-05-11 | T | flyout action wiring | mitigate | Keep delete/regenerate actions routed through the existing slot mutation handlers so the flyout cannot desynchronize from the grid |
| T-05-12 | R | stale `PLAN-02` wording | mitigate | Add explicit unit/E2E assertions that no inline edit affordance exists, documenting the D-16/D-17 traceability reconciliation in executable tests |
</threat_model>

<verification>
- `npx vitest run src/components/generation/meal-detail-flyout.test.tsx`
- `npx playwright test tests/e2e/plan-management.spec.ts`
- `rg -n 'Why this fits|View details|no inline edit' src/components/generation tests/e2e`
</verification>

<success_criteria>
- A right-side flyout exposes description and rationale while the grid remains visible behind it.
- Keyboard focus moves into, remains within, and returns from the flyout correctly.
- Phase 5 explicitly guards against inline title editing even though `PLAN-02` wording is stale.
</success_criteria>

<output>
After completion, create `.planning/phases/05-meal-plan-grid-and-management/05-04-SUMMARY.md`
</output>
