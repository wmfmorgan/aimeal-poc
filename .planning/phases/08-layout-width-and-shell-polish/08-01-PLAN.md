---
phase: 08-layout-width-and-shell-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout/AppFrame.tsx
autonomous: true
requirements:
  - LAY-01
  - UI-02
must_haves:
  truths:
    - "Users see slimmer outer chrome across the app before any route-specific cleanup lands."
    - "The shared shell feels editorial and centered, not full-bleed or dashboard-like."
    - "Header and nav spacing stop consuming disproportionate width above routed content."
  artifacts:
    - path: src/app/layout/AppFrame.tsx
      provides: "Widened centered shell, reduced viewport gutters, tighter header rhythm"
      min_lines: 80
  key_links:
    - from: src/app/layout/AppFrame.tsx
      to: routed pages
      via: Outlet shell wrapper
      pattern: "max-w-\\[88rem\\]|xl:px-8|md:px-6"
---

<objective>
Establish the shared wide-shell contract in `AppFrame` so every route inherits slimmer chrome before route-level cleanup begins.

Purpose: Implement D-01, D-02, and D-03 by reclaiming width globally without abandoning the existing editorial shell.
Output: Updated `AppFrame` spacing, max-width, header rhythm, and nav pill spacing that downstream plans can build on.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md
@.planning/phases/08-layout-width-and-shell-polish/08-RESEARCH.md
@.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md
@.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md
@src/app/layout/AppFrame.tsx

<interfaces>
From `src/app/layout/AppFrame.tsx`:

```tsx
export function AppFrame()
```

Current shell seam:

```tsx
<div className="min-h-screen bg-transparent px-5 py-6 ... md:px-10 md:py-8">
  <div className="mx-auto ... max-w-6xl ... p-4 ... md:p-8">
    <header className="mb-10 ...">
      ...
    </header>
    <main className="flex-1">
      <Outlet />
    </main>
  </div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the shared shell and tighten outer gutters</name>
  <read_first>
    - `src/app/layout/AppFrame.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-01 through D-03)
    - `.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md` (AppFrame outer shell width + chrome pattern)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (App shell layout contract)
  </read_first>
  <files>src/app/layout/AppFrame.tsx</files>
  <action>Update the outer viewport padding to the Phase 8 contract values: `px-4 py-5` on mobile, `md:px-6 md:py-6` on tablet, and `xl:px-8 xl:py-7` on desktop. Increase the centered shell width from `max-w-6xl` to the inline `max-w-[88rem]` contract resolved in `08-RESEARCH.md`, and reduce shell inner padding to `p-4 md:p-6 xl:p-7` so reclaimed width benefits route content rather than just the glass shell chrome. Follow the `AppFrame` analog in `08-PATTERNS.md` for the existing shell structure, and preserve the rounded, translucent, blurred editorial shell per D-02; do not convert the app to full-bleed or add new layout abstractions.</action>
  <acceptance_criteria>
    - `src/app/layout/AppFrame.tsx` contains `max-w-[88rem]`.
    - The outer wrapper uses `px-4`, `md:px-6`, and `xl:px-8` responsive gutter classes.
    - The shell wrapper uses `p-4`, `md:p-6`, and `xl:p-7` responsive inner padding classes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/plan-page.test.tsx src/routes/auth-page.test.tsx</automated>
  </verify>
  <done>The app shell is globally wider and slimmer, and every routed page now mounts inside the shared Phase 8 width contract.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce header and nav rhythm so routed content wins the width hierarchy</name>
  <read_first>
    - `src/app/layout/AppFrame.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-04 and D-05 for content hierarchy carry-through)
    - `.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md` (AppFrame header hierarchy to preserve while tightening)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (header-to-main spacing and nav spacing)
  </read_first>
  <files>src/app/layout/AppFrame.tsx</files>
  <action>Tighten the `header` block so it no longer spends oversized vertical space before the route outlet. Reduce the header-to-main gap to the contract target of roughly `24px` mobile and `32px` desktop, tighten the headline/copy stack, and keep nav pills compact while preserving the 44px minimum target. Follow the `AppFrame` header hierarchy analog in `08-PATTERNS.md` so the shell remains recognizably editorial while content regains priority. Keep the sign-out affordance visually quiet. Do not rewrite copy, alter navigation destinations, or change auth behavior.</action>
  <acceptance_criteria>
    - `src/app/layout/AppFrame.tsx` no longer uses `mb-10` on the shell header.
    - The nav pill cluster retains button-height-safe classes while using tighter spacing than the current `gap-2` plus oversized header stack.
    - The `Outlet` remains rendered directly inside the shared shell `main`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/plan-page.test.tsx src/routes/auth-page.test.tsx</automated>
  </verify>
  <done>Shared shell rhythm is tighter, the header remains editorial, and routed pages inherit more usable vertical and horizontal space.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser viewport -> shared shell | Unpredictable viewport sizes can make widened shells unreadable if spacing is not constrained |
| Shared shell -> routed page outlet | Any shell regression cascades into every route mounted inside `Outlet` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | D | `src/app/layout/AppFrame.tsx` | mitigate | Constrain width with `max-w-[88rem]` and responsive gutter classes instead of full-bleed expansion so large viewports do not degrade readability |
| T-08-02 | T | `src/app/layout/AppFrame.tsx` | mitigate | Keep shell changes isolated to spacing/max-width classes only; do not alter navigation targets or auth wiring while changing layout |
| T-08-03 | I | shared shell/header | accept | No new data is exposed in Phase 8; widened chrome changes only presentation, not data access or visibility rules |
</threat_model>

<verification>
Run `npm run test:unit -- --run src/routes/plan-page.test.tsx src/routes/auth-page.test.tsx` after the shell refactor to catch route-mount regressions before route-specific cleanup begins.
</verification>

<success_criteria>
`AppFrame` provides the approved wide-shell contract, all routes inherit slimmer chrome, and the shared header no longer dominates the page before route content renders.
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-width-and-shell-polish/08-01-SUMMARY.md`
</output>
