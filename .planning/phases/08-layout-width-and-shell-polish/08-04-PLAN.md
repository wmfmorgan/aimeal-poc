---
phase: 08-layout-width-and-shell-polish
plan: 04
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - src/routes/plan-page.test.tsx
  - src/routes/home-page.test.tsx
  - src/routes/household-page.test.tsx
  - src/routes/dev-page.test.tsx
  - src/routes/auth-page.test.tsx
  - tests/e2e/layout-shell.spec.ts
autonomous: false
requirements:
  - LAY-01
  - LAY-02
  - UI-02
must_haves:
  truths:
    - "Automated tests catch shell-width and plan-layout regressions introduced by Phase 8."
    - "Responsive verification explicitly covers desktop 4-day, desktop 7-day, tablet, and mobile plan layouts."
    - "Manual signoff happens after automation, not in place of it."
  artifacts:
    - path: src/routes/plan-page.test.tsx
      provides: "Route assertions for shell/layout hierarchy and plan density seams"
      min_lines: 650
    - path: src/routes/home-page.test.tsx
      provides: "Home route shell-width and spacing regression assertions"
      min_lines: 40
    - path: src/routes/household-page.test.tsx
      provides: "Household route shell-width and spacing regression assertions"
      min_lines: 50
    - path: src/routes/dev-page.test.tsx
      provides: "Dev route spacing/layout regression coverage"
      min_lines: 120
    - path: src/routes/auth-page.test.tsx
      provides: "Auth route focused-shell regression coverage"
      min_lines: 320
    - path: tests/e2e/layout-shell.spec.ts
      provides: "Viewport-specific responsive verification for plan and shell layouts"
      min_lines: 80
  key_links:
    - from: tests/e2e/layout-shell.spec.ts
      to: playwright.config.ts
      via: baseURL-driven viewport checks
      pattern: "testDir|baseURL"
    - from: src/routes/plan-page.test.tsx
      to: src/routes/plan-page.tsx
      via: rendered route assertions
      pattern: "PlanPage"
---

<objective>
Lock Phase 8 down with targeted automated regression coverage and a final human verification gate focused on responsiveness and overall shell feel.

Purpose: Provide the verification required by the roadmap while keeping manual visual checks in one final plan instead of spreading them across implementation work.
Output: Updated Vitest assertions, a focused Playwright layout spec, and a blocking human verification checkpoint.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md
@.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md
@.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md
@playwright.config.ts
@src/routes/plan-page.test.tsx
@src/routes/home-page.test.tsx
@src/routes/household-page.test.tsx
@src/routes/dev-page.test.tsx
@src/routes/auth-page.test.tsx
@tests/e2e/generation-flow.spec.ts
@tests/e2e/finalization-favorites.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add automated layout regression coverage for shell and responsive plan density</name>
  <read_first>
    - `src/routes/plan-page.test.tsx`
    - `src/routes/home-page.test.tsx`
    - `src/routes/household-page.test.tsx`
    - `src/routes/dev-page.test.tsx`
    - `src/routes/auth-page.test.tsx`
    - `tests/e2e/generation-flow.spec.ts`
    - `tests/e2e/finalization-favorites.spec.ts`
    - `.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md` (mapped route analogs for AppFrame, home, household, dev, and auth)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (Verification targets)
  </read_first>
  <files>src/routes/plan-page.test.tsx, src/routes/home-page.test.tsx, src/routes/household-page.test.tsx, src/routes/dev-page.test.tsx, src/routes/auth-page.test.tsx, tests/e2e/layout-shell.spec.ts</files>
  <action>Add targeted Vitest assertions for the new shell/container tokens and plan-page hierarchy seams, and keep the route-level assertions aligned to the mapped analogs in `08-PATTERNS.md` so tests verify the intended home narrative cap, household primary/support width balance, dev card stack spacing, and auth focused-shell cap. Then create `tests/e2e/layout-shell.spec.ts` with explicit viewport checks for desktop 4-day, desktop 7-day, tablet, and mobile plan layouts plus spot checks that `/`, `/household`, and `/auth` preserve the tighter shell contract. Reuse existing e2e helpers and route setup patterns instead of inventing a new harness. Keep the Playwright assertions focused on width, overflow, and spacing behavior, not on Phase 9 card-content changes.</action>
  <acceptance_criteria>
    - `tests/e2e/layout-shell.spec.ts` exists and contains viewport coverage for desktop 4-day, desktop 7-day, tablet, and mobile.
    - `src/routes/plan-page.test.tsx` gains assertions tied to Phase 8 shell/layout composition rather than only plan data wiring.
    - `src/routes/home-page.test.tsx` and `src/routes/household-page.test.tsx` each include at least one Phase 8 layout regression assertion.
    - `src/routes/dev-page.test.tsx` and `src/routes/auth-page.test.tsx` each include at least one Phase 8 layout regression assertion.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/plan-page.test.tsx src/routes/home-page.test.tsx src/routes/household-page.test.tsx src/routes/dev-page.test.tsx src/routes/auth-page.test.tsx && npm run test:e2e -- tests/e2e/layout-shell.spec.ts</automated>
  </verify>
  <done>Automated regression coverage exists for the widened shell, route spacing, and responsive plan layout targets introduced in Phase 8.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the widened shell feels intentional across representative viewports</name>
  <read_first>
    - `.planning/ROADMAP.md` (Phase 8 testing and success criteria)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (Verification targets)
    - `tests/e2e/layout-shell.spec.ts`
  </read_first>
  <acceptance_criteria>
    - The user verifies desktop 4-day and 7-day plan views feel less cramped than the Phase 7 baseline.
    - The user verifies tablet keeps a meaningful matrix layout before overflow.
    - The user verifies mobile remains structurally familiar while spacing is tighter on plan, auth, and household routes.
  </acceptance_criteria>
  <what-built>Automated coverage for Phase 8 shell spacing plus a dedicated responsive Playwright spec for the widened plan layout.</what-built>
  <how-to-verify>
    1. Run the app locally through `netlify dev` so Playwright and manual route checks share the expected `http://127.0.0.1:8888` base URL.
    2. Inspect `/plan/:id` with a 4-day plan at desktop width and confirm the grid has visibly more breathing room than the previous shell.
    3. Inspect `/plan/:id` with a 7-day plan at desktop width and confirm gutters tighten intentionally without immediately defaulting to horizontal overflow.
    4. Inspect the plan route at tablet width and confirm the matrix layout still uses width meaningfully rather than collapsing into a mobile-feeling stack.
    5. Inspect `/` and `/household` and confirm the tighter shell still preserves the home editorial split plus the household primary/support rail hierarchy rather than stretching them.
    6. Inspect mobile plan, household, and auth layouts and confirm spacing is tighter but structure and readability remain intact.
  </how-to-verify>
  <verify>
    <automated>npm run test:unit -- --run src/routes/plan-page.test.tsx src/routes/home-page.test.tsx src/routes/household-page.test.tsx src/routes/dev-page.test.tsx src/routes/auth-page.test.tsx && npm run test:e2e -- tests/e2e/layout-shell.spec.ts</automated>
  </verify>
  <resume-signal>Type `approved` if the layout passes, or list the viewport and route issues that need correction.</resume-signal>
  <done>Phase 8 has both automated and human-confirmed evidence that shell width, route spacing, and responsive plan density meet the milestone bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated test harness -> browser layout | The test suite must verify real viewport behavior rather than only mocked DOM structure |
| Human verification -> milestone signoff | Manual approval should happen only after automation passes so subjective review does not mask regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | R | verification workflow | mitigate | Require both targeted Vitest and Playwright commands before the human verification checkpoint is considered complete |
| T-08-11 | D | `tests/e2e/layout-shell.spec.ts` | mitigate | Cover desktop 4-day, desktop 7-day, tablet, and mobile explicitly so viewport regressions are reproducible rather than anecdotal |
| T-08-12 | T | test expectations | accept | Phase 8 tests intentionally target spacing/layout contracts only; card-content and flyout assertions remain deferred to later phases |
</threat_model>

<verification>
Phase verification is complete when `npm run test:unit -- --run src/routes/plan-page.test.tsx src/routes/home-page.test.tsx src/routes/household-page.test.tsx src/routes/dev-page.test.tsx src/routes/auth-page.test.tsx` and `npm run test:e2e -- tests/e2e/layout-shell.spec.ts` both pass, followed by checkpoint approval.
</verification>

<success_criteria>
Automated coverage exists for the new shell and layout seams, and a human has signed off on desktop 4-day, desktop 7-day, tablet, and mobile responsiveness after automation passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-width-and-shell-polish/08-04-SUMMARY.md`
</output>
