---
phase: 08-layout-width-and-shell-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/routes/home-page.tsx
  - src/routes/home-page.test.tsx
  - src/routes/household-page.tsx
  - src/routes/household-page.test.tsx
  - src/routes/dev-page.tsx
  - src/routes/auth-page.tsx
autonomous: true
requirements:
  - LAY-01
  - UI-02
must_haves:
  truths:
    - "Home, household, dev, and auth inherit the wider shell without feeling stretched."
    - "Reading-heavy routes keep deliberate internal max widths inside the wider shell."
    - "Mobile and tablet spacing across non-plan routes is tighter and less wasteful than the Phase 7 baseline."
  artifacts:
    - path: src/routes/home-page.tsx
      provides: "Home route spacing and width-cap cleanup"
      min_lines: 70
    - path: src/routes/home-page.test.tsx
      provides: "Home route layout regression coverage for shell-width cleanup"
      min_lines: 40
    - path: src/routes/household-page.tsx
      provides: "Long-form household route rhythm tuned for wider shell"
      min_lines: 500
    - path: src/routes/household-page.test.tsx
      provides: "Household route layout regression coverage for width-cap and spacing cleanup"
      min_lines: 50
    - path: src/routes/dev-page.tsx
      provides: "Dev route card stack that fills widened shell intentionally"
      min_lines: 120
    - path: src/routes/auth-page.tsx
      provides: "Focused auth composition with reduced wasted perimeter"
      min_lines: 400
  key_links:
    - from: src/app/layout/AppFrame.tsx
      to: src/routes/home-page.tsx
      via: shared shell plus route-local width caps
      pattern: "max-w-"
    - from: src/app/layout/AppFrame.tsx
      to: src/routes/auth-page.tsx
      via: centered auth form within widened shell
      pattern: "max-w-\\[32rem\\]"
---

<objective>
Apply the broader page-level spacing cleanup to the non-plan routes so the shell-wide width strategy feels coherent across the app.

Purpose: Implement D-13 through D-15 and finish the route-level portion of `LAY-01` and `UI-02` without leaking into meal-card or flyout redesign work.
Output: Updated home, household, dev, and auth route composition tuned for the wider shell.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md
@.planning/phases/08-layout-width-and-shell-polish/08-RESEARCH.md
@.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md
@.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md
@src/routes/home-page.tsx
@src/routes/household-page.tsx
@src/routes/dev-page.tsx
@src/routes/auth-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance home and dev route spacing inside the wider shell</name>
  <read_first>
    - `src/routes/home-page.tsx`
    - `src/routes/dev-page.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md` (home editorial split analog, dev vertical stack analog)
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-13 through D-15)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (Route-by-route application for `/` and `/dev`)
  </read_first>
  <files>src/routes/home-page.tsx, src/routes/home-page.test.tsx, src/routes/dev-page.tsx</files>
  <action>Tighten section gaps, card padding, and reading widths on home and dev so they use the widened shell intentionally. Keep the editorial split on home, but cap narrative measure and reduce dead perimeter using the `src/routes/home-page.tsx` analog called out in `08-PATTERNS.md`. On dev, let the dense cards use more horizontal space while preserving each card boundary and existing data presentation using the existing vertical stack analog in `08-PATTERNS.md`. Add or extend `src/routes/home-page.test.tsx` so `/` has explicit Phase 8 layout assertions for the tightened hero measure and shell spacing. Do not change route purpose, copy meaning, or data-fetch behavior.</action>
  <acceptance_criteria>
    - `src/routes/home-page.tsx` keeps the split layout but adds or tightens internal max-width control for hero copy.
    - `src/routes/dev-page.tsx` no longer uses the same oversized `px-8 py-8` shell spacing as the Phase 7 baseline.
    - Both routes preserve their current headings and CTA destinations.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/home-page.test.tsx src/routes/dev-page.test.tsx</automated>
  </verify>
  <done>Home and dev visibly benefit from the wider shell while still reading like editorial pages rather than dashboard widgets.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten household form rhythm without breaking the long-form editing flow</name>
  <read_first>
    - `src/routes/household-page.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md` (household hero + support rail analog)
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-13 through D-15)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (`/household` width caps and spacing)
  </read_first>
  <files>src/routes/household-page.tsx, src/routes/household-page.test.tsx</files>
  <action>Reduce oversized section gaps, tighten hero and section shell padding, and keep the primary form rail capped near the contract width while the support rail remains secondary. Follow the `src/routes/household-page.tsx` analog in `08-PATTERNS.md` so the existing hero-plus-support-rail composition survives the cleanup. Add or extend `src/routes/household-page.test.tsx` with explicit assertions for the tightened main form shells and primary/support width balance. Preserve the existing validation, member-editing flow, submit banners, and all field semantics. Do not refactor form behavior or add new affordances; this is layout cleanup only.</action>
  <acceptance_criteria>
    - `src/routes/household-page.tsx` no longer relies on blanket `space-y-8` plus `px-8 py-8` for the main form shells.
    - The hero/support rail structure remains intact with explicit primary and secondary width balance.
    - Existing form labels, buttons, and validation roles remain present.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/household-page.test.tsx</automated>
  </verify>
  <done>The household route feels continuous and less overframed inside the new shell while preserving the established editing flow.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the focused auth form while removing wasted perimeter space</name>
  <read_first>
    - `src/routes/auth-page.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md` (auth centered single-surface analog)
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-13 through D-15)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (`/auth` max 32rem and perimeter cleanup)
  </read_first>
  <files>src/routes/auth-page.tsx</files>
  <action>Keep `/auth` centered and capped at `32rem`, but reduce the surrounding shell padding and internal spacing so the route no longer feels overframed after `AppFrame` widens. Follow the centered single-surface analog in `08-PATTERNS.md` so the auth page stays visually focused while the perimeter tightens. Preserve all auth modes, form copy, connectivity proof behavior, and success/error messaging. Do not widen the form beyond the contract or change auth logic.</action>
  <acceptance_criteria>
    - `src/routes/auth-page.tsx` still contains a single centered form shell with `max-w-[32rem]`.
    - The auth route uses tighter outer spacing than the current `min-h-[60vh] ... px-4 py-8` baseline.
    - Existing auth tests can still target the same headings, placeholders, and buttons.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/auth-page.test.tsx</automated>
  </verify>
  <done>The auth route remains focused and calm, but no longer wastes perimeter space inside the widened shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared shell -> non-plan routes | Route-specific cleanup must not over-stretch prose, forms, or dense utility cards |
| Browser viewport -> auth/household forms | Tighter spacing must still preserve readable and accessible form layouts on mobile/tablet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | D | `src/routes/household-page.tsx` | mitigate | Keep explicit primary/support rail widths and retain mobile-first stacking so wider shell changes do not degrade long-form readability |
| T-08-08 | D | `src/routes/auth-page.tsx` | mitigate | Preserve the `max-w-[32rem]` auth cap while reducing perimeter spacing so the form does not stretch after shell widening |
| T-08-09 | T | `src/routes/home-page.tsx`, `src/routes/dev-page.tsx` | accept | Route cleanup touches layout classes only; no user-controlled content or new data paths are introduced in this phase |
</threat_model>

<verification>
Run `npm run test:unit -- --run src/routes/home-page.test.tsx src/routes/household-page.test.tsx src/routes/dev-page.test.tsx src/routes/auth-page.test.tsx` after non-plan route cleanup.
</verification>

<success_criteria>
All non-plan shell routes feel aligned with the wider app shell, mobile/tablet spacing is tighter, and home plus household have matching automated coverage instead of relying only on auth and dev regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-width-and-shell-polish/08-03-SUMMARY.md`
</output>
