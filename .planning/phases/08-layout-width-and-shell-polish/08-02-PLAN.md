---
phase: 08-layout-width-and-shell-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/routes/plan-page.tsx
  - src/components/generation/GenerationForm.tsx
  - src/components/generation/PlanFinalizationCard.tsx
  - src/components/generation/MealPlanGrid.tsx
autonomous: true
requirements:
  - LAY-02
  - UI-02
must_haves:
  truths:
    - "The persisted and generation plan routes give the meal grid the majority of reclaimed width."
    - "Four-day plans stay relaxed while six- and seven-day plans tighten density without immediately overflowing."
    - "Tablet keeps a meaningful grid layout instead of behaving like an oversized mobile stack."
  artifacts:
    - path: src/routes/plan-page.tsx
      provides: "Compact plan-page section rhythm and grid-first composition"
      min_lines: 200
    - path: src/components/generation/MealPlanGrid.tsx
      provides: "Day-count-aware desktop/tablet density rules"
      min_lines: 160
    - path: src/components/generation/GenerationForm.tsx
      provides: "Generation route shell aligned with persisted plan spacing"
      min_lines: 100
    - path: src/components/generation/PlanFinalizationCard.tsx
      provides: "Subordinate finalization surface within wider plan workspace"
      min_lines: 80
  key_links:
    - from: src/routes/plan-page.tsx
      to: src/components/generation/MealPlanGrid.tsx
      via: grid wrapper section
      pattern: "MealPlanGrid"
    - from: src/components/generation/MealPlanGrid.tsx
      to: numDays
      via: adaptive gap and column sizing
      pattern: "numDays|days.length"
---

<objective>
Rebalance the plan surfaces so `/plan/:id` and `/plan/new` use the wider shell intentionally, with the meal grid as the primary beneficiary.

Purpose: Implement D-04 through D-12 and requirement `LAY-02` by tightening plan-page composition and adding adaptive grid density without leaking card or flyout refactors into Phase 8.
Output: Updated plan route composition, aligned generation form spacing, a less dominant finalization surface, and day-count-aware grid density rules.
</objective>

<execution_context>
@/Users/jabroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jabroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md
@.planning/phases/08-layout-width-and-shell-polish/08-RESEARCH.md
@.planning/phases/08-layout-width-and-shell-polish/08-PATTERNS.md
@.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md
@src/routes/plan-page.tsx
@src/components/generation/GenerationForm.tsx
@src/components/generation/PlanFinalizationCard.tsx
@src/components/generation/MealPlanGrid.tsx

<interfaces>
From `src/components/generation/MealPlanGrid.tsx`:

```tsx
type MealPlanGridProps = {
  numDays: number;
  mealTypes: string[];
  slots: Record<string, MealSlot | MealPlanSlot>;
  ...
};

export function MealPlanGrid({...}: MealPlanGridProps)
```

Current desktop seam:

```tsx
<div
  className="hidden gap-4 md:grid"
  data-testid="meal-plan-grid-desktop"
  style={{ gridTemplateColumns: `minmax(6rem, auto) repeat(${days.length}, minmax(0, 1fr))` }}
>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten plan-route section rhythm so the grid becomes the dominant surface</name>
  <read_first>
    - `src/routes/plan-page.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-04 through D-06)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (Plan page hierarchy)
  </read_first>
  <files>src/routes/plan-page.tsx</files>
  <action>Reduce the top-level plan-page stack from the current blanket `space-y-8` to a tighter responsive rhythm, compact the plan header shell, keep the `Create new plan` action right-aligned and secondary, and reduce grid wrapper padding so the `MealPlanGrid` gains more of the reclaimed shell width. Preserve the existing section order, persisted-plan workflow, favorites/shopping-list/flyout wiring, and overlay structure. Do not change card actions, flyout content, or selection behavior.</action>
  <acceptance_criteria>
    - `src/routes/plan-page.tsx` no longer uses the same `px-8 py-8` shell spacing for every plan section.
    - The top-level plan page container no longer uses a blanket `space-y-8` for both generation and persisted views.
    - The grid section still renders `MealPlanGrid` in both persisted and generation flows.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/plan-page.test.tsx</automated>
  </verify>
  <done>The plan route reads as grid-first, with compact support surfaces above it and unchanged plan-management behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Align generation and finalization shells with the compact plan hierarchy</name>
  <read_first>
    - `src/components/generation/GenerationForm.tsx`
    - `src/components/generation/PlanFinalizationCard.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-05 and D-06)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (Shared route composition, Plan page hierarchy)
  </read_first>
  <files>src/components/generation/GenerationForm.tsx, src/components/generation/PlanFinalizationCard.tsx</files>
  <action>Bring `GenerationForm` onto the same tighter section rhythm as the persisted plan view so `/plan/new` no longer feels like an older layout system. In `PlanFinalizationCard`, rebalance the desktop split so the text block caps near the contract width, actions remain compact, and the finalization surface supports the grid instead of competing with it. Keep the existing copy, buttons, and finalization/favorites behavior intact.</action>
  <acceptance_criteria>
    - `src/components/generation/GenerationForm.tsx` uses tighter section/form spacing than the current `px-8 py-8` plus `mt-8 space-y-8` defaults.
    - `src/components/generation/PlanFinalizationCard.tsx` constrains its text column and keeps the action stack compact on desktop.
    - Neither file introduces new CTA labels or new behavior branches.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/routes/plan-page.test.tsx</automated>
  </verify>
  <done>Generation and persisted plan shells feel like one system, and the finalization card stays visibly subordinate to the grid.</done>
</task>

<task type="auto">
  <name>Task 3: Add day-count-aware density rules to the desktop and tablet meal grid</name>
  <read_first>
    - `src/components/generation/MealPlanGrid.tsx`
    - `.planning/phases/08-layout-width-and-shell-polish/08-CONTEXT.md` (D-07 through D-12)
    - `.planning/phases/08-layout-width-and-shell-polish/08-UI-SPEC.md` (Responsive composition)
  </read_first>
  <files>src/components/generation/MealPlanGrid.tsx</files>
  <action>Keep the existing mobile stacked branch, but add explicit density rules for the `md` grid branch so four-day layouts stay relatively open, five-day layouts tighten moderately, and six- to seven-day layouts use visibly smaller gutters and label spacing before horizontal overflow is considered. Preserve the existing meal-type left rail and day columns. Do not alter meal-card content, meal labels inside cards, or click behavior; this task is limited to wrapper density, gap, and column math per D-07 through D-09.</action>
  <acceptance_criteria>
    - `src/components/generation/MealPlanGrid.tsx` contains explicit day-count branching for density classes or style values.
    - `data-testid="meal-plan-grid-mobile"` remains present and still renders the stacked mobile model.
    - `data-testid="meal-plan-grid-desktop"` remains present and still uses the matrix layout with `days.length` in the column template.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --run src/components/generation/meal-plan-management.test.tsx src/routes/plan-page.test.tsx</automated>
  </verify>
  <done>The grid adapts spacing by day count across desktop and tablet while preserving the current mobile structure and management flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route params/state -> plan layout | `numDays`, `mealTypes`, and persisted plan state drive layout density and section composition |
| Plan route -> overlays | Plan layout changes must not break flyout, favorites, or shopping-list triggers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | D | `src/components/generation/MealPlanGrid.tsx` | mitigate | Make density responsive to `numDays` so 6-7 day layouts do not collapse into unreadable columns or overflow by default |
| T-08-05 | T | `src/routes/plan-page.tsx` | mitigate | Restrict changes to container spacing and section hierarchy; preserve existing plan mutations, overlay state, and component wiring |
| T-08-06 | E | plan management actions | accept | No new privileged actions are introduced; Phase 8 reuses existing buttons and mutation flows without expanding authority |
</threat_model>

<verification>
Run `npm run test:unit -- --run src/routes/plan-page.test.tsx src/components/generation/meal-plan-management.test.tsx` after the route and grid changes.
</verification>

<success_criteria>
`/plan/:id` and `/plan/new` visibly prioritize the meal grid, adaptive density responds to 4-day through 7-day layouts, and the phase stays strictly in spacing, composition, and density territory.
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-width-and-shell-polish/08-02-SUMMARY.md`
</output>
