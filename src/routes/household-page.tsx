export function HouseholdPage() {
  return (
    <section className="max-w-3xl space-y-4 rounded-[1.75rem] bg-white/72 p-8">
      <p className="text-xs uppercase tracking-[0.26em] text-[var(--color-muted)]">
        Household surface
      </p>
      <h2 className="font-display text-4xl text-[var(--color-sage-deep)]">Household placeholder</h2>
      <p className="text-sm leading-7 text-[var(--color-muted)]">
        Phase 3 will capture members, appliances, allergies, avoidances, and cooking skill. This
        placeholder keeps the navigation and route contract stable from the first execution pass.
      </p>
    </section>
  );
}
